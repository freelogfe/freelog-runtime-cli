# Freelog CLI 脚手架需求与设计文档

> 版本: v1.0  
> 更新日期: 2025-10-30

## 目录

- [需求分析](#需求分析)
- [设计方案](#设计方案)
- [命令参考](#命令参考)
- [配置文件结构](#配置文件结构)
- [错误处理](#错误处理)
- [开发计划](#开发计划)

---

## 需求分析

### 基本需求

- 创建项目模版
- 提供完善的帮助文档
- 支持本地和全局配置
- 提供友好的交互式命令行界面

### 1. 身份认证管理

#### 1.1 全局登录
- 登录凭证存储在用户主目录
- 适用于个人开发者日常使用
- 跨项目共享登录状态

#### 1.2 工作空间登录
- 登录凭证存储在项目根目录
- 适用于团队协作场景
- 项目级别的身份隔离
- 优先级高于全局登录

#### 1.3 登录状态管理
- 查看当前登录状态
- 支持同时查看全局和工作空间登录信息
- 登出功能（全局/工作空间）

### 2. 作品发布

#### 2.1 发布到草稿
- 保存未完成的作品版本
- 不对外公开，仅作者可见
- 支持多次修改和覆盖

#### 2.2 发布为正式版本
- 版本号自动校验和递增
- 自动打包和上传作品文件
- 生成版本发布记录
- 支持语义化版本管理

#### 2.3 发布选项
- 指定发布用户（全局/工作空间）
- 指定作品文件路径
- 指定配置文件路径
- 支持自定义发布参数

### 3. 依赖管理

> **注意**: 目前不支持对依赖进行配置，未来版本可能会添加依赖配置功能

#### 3.1 添加依赖
- 通过作品ID、名称或URL添加依赖
- 支持指定版本号或使用latest
- **添加流程**:
  1. 拉取并展示可用策略
  2. 如已授权，可跳过签约流程
  3. 选择策略进行签约或选择上抛
  4. 完成支付流程（如需要）
  5. 将依赖信息写入配置文件

#### 3.2 修改依赖
- **修改方式**:
  - 合约应用修改（调整现有合约参数）
  - 重新选择策略进行签约
- **修改流程**:
  1. 选择修改方式
  2. 如选择合约修改，支持上抛操作
  3. 如选择重新签约，执行添加依赖流程
  4. 更新配置文件中的依赖信息

#### 3.3 删除依赖
- 支持批量删除
- 删除前确认提示
- 自动更新配置文件

#### 3.4 版本更新
- 更新单个或多个依赖到指定版本
- 省略版本号则更新到最新版本
- 显示更新前后的版本对比

#### 3.5 依赖查询
- 查询线上指定版本的依赖列表
- 显示依赖的授权状态
- 显示依赖的版本信息

#### 3.6 依赖同步
- 从线上同步依赖列表到本地
- 支持指定版本或最新版本
- 自动更新本地配置文件

### 4. 信息同步

#### 4.1 同步作品信息
- 拉取线上作品基本信息
- 创建或覆盖本地 freelog.json
- 用于初始化本地项目

#### 4.2 同步完整信息
- 同步指定版本的所有信息
- 包含作品元数据、依赖、属性、配置等
- 支持选择最新版本或指定版本

#### 4.3 同步部分信息
- 仅同步属性信息
- 仅同步可选配置
- 仅同步更新说明
- 灵活的选择性同步

### 5. 作品文件属性分析

#### 5.1 文件分析
- 自动分析上传文件的属性
- 智能识别文件类型和结构
- 提取关键元数据信息

#### 5.2 属性管理
- 添加自定义属性
- 编辑现有属性
- 属性验证和格式化

## 设计方案

### 1. 身份认证管理

#### 1.1 登录命令

**全局登录**
```bash
freelog-cli login -g
# 或
freelog-cli login --global
```

**工作空间登录**
```bash
freelog-cli login
```

**登录参数**
- `-u, --username <username>` - 用户名
- `-p, --password <password>` - 密码
- `-g, --global` - 全局登录标志

**示例**
```bash
# 交互式全局登录
freelog-cli login -g

# 直接传参登录
freelog-cli login -u myuser -p mypassword

# 工作空间登录
freelog-cli login -u myuser -p mypassword
```

#### 1.2 登出命令

**全局登出**
```bash
freelog-cli logout -g
# 或
freelog-cli logout --global
```

**工作空间登出**
```bash
freelog-cli logout
```

#### 1.3 查看登录状态

**查看所有登录状态**
```bash
freelog-cli login status
```
显示全局和工作空间的登录信息

**仅查看全局登录状态**
```bash
freelog-cli login status -g
# 或
freelog-cli login status --global
```

**输出示例**
```
全局登录:
  用户名: user@example.com
  登录时间: 2025-10-30 10:00:00
  Token 有效期: 30 天

工作空间登录:
  用户名: team@example.com
  登录时间: 2025-10-30 11:00:00
  Token 有效期: 7 天
  项目: my-freelog-project
```

---

### 2. 作品发布

#### 2.1 基本发布命令

```bash
freelog-cli publish [options]
```

#### 2.2 发布选项

**用户选择**
- `-gu, --global-user` - 使用全局登录用户发布
- `-wu, --workspace-user` - 使用工作空间登录用户发布

**发布模式**
- `-d, --draft` - 发布为草稿

**文件指定**
- `-f, --file <path>` - 指定作品文件路径
- `-c, --config <path>` - 指定配置文件路径

**版本控制**
- `-m, --message <message>` - 版本更新说明
- `--major` - 主版本号递增
- `--minor` - 次版本号递增
- `--patch` - 补丁版本号递增

#### 2.3 发布示例

**基本发布**
```bash
# 发布正式版本（交互式选择用户）
freelog-cli publish

# 使用全局用户发布
freelog-cli publish -gu

# 使用工作空间用户发布
freelog-cli publish -wu
```

**草稿发布**
```bash
# 发布草稿
freelog-cli publish -d

# 使用全局用户发布草稿
freelog-cli publish -d -gu
```

**指定文件发布**
```bash
# 指定作品文件
freelog-cli publish -f ./dist/bundle.zip

# 指定配置文件
freelog-cli publish -c ./custom-config.json

# 同时指定作品文件和配置文件
freelog-cli publish -f ./dist/bundle.zip -c ./custom-config.json -gu
```

**版本控制发布**
```bash
# 自动递增补丁版本号
freelog-cli publish --patch -m "修复登录问题"

# 递增次版本号
freelog-cli publish --minor -m "新增用户管理功能"

# 递增主版本号
freelog-cli publish --major -m "重构架构"
```

#### 2.4 发布流程

1. **用户验证**: 若未指定 `-gu` 或 `-wu`，提示选择已登录账户
2. **版本检查**: 自动检查版本号是否合法，是否需要递增
3. **文件打包**: 根据配置文件或指定路径打包作品文件
4. **信息验证**: 验证作品信息完整性
5. **上传文件**: 上传作品文件到服务器
6. **发布确认**: 显示发布结果和版本信息

---

### 3. 依赖管理

#### 3.1 添加依赖

**命令格式**
```bash
freelog-cli add <resource>[@version]
```

**参数说明**
- `<resource>` - 作品ID、名称、唯一标识符或URL
- `[@version]` - 可选，指定版本号或使用 `latest`

**示例**
```bash
# 添加最新版本依赖
freelog-cli add resource-id

# 添加指定版本依赖
freelog-cli add resource-id@1.0.0

# 通过名称添加
freelog-cli add my-resource@latest

# 通过URL添加
freelog-cli add https://freelog.com/resource/12345@2.1.0
```

**交互流程**
1. 查询资源信息
2. 展示可用策略列表
3. 选择策略（如已授权可跳过）
4. 签约或选择上抛
5. 完成支付（如需要）
6. 写入配置文件

#### 3.2 修改依赖

**命令格式**
```bash
freelog-cli change <resource>[@version]
```

**示例**
```bash
# 修改依赖（交互式选择修改方式）
freelog-cli change resource-id

# 修改并指定新版本
freelog-cli change resource-id@2.0.0
```

**修改流程**
1. 选择修改方式：
   - 合约应用修改
   - 重新选择策略签约
2. 执行相应的修改操作
3. 更新配置文件

#### 3.3 删除依赖

**命令格式**
```bash
freelog-cli remove <resources...>
```

**示例**
```bash
# 删除单个依赖
freelog-cli remove resource-id

# 批量删除依赖
freelog-cli remove resource-1 resource-2 resource-3

# 删除指定版本
freelog-cli remove resource-id@1.0.0
```

**删除流程**
1. 显示待删除的依赖列表
2. 确认删除操作
3. 从配置文件中移除
4. 显示删除结果

#### 3.4 更新依赖版本

**命令格式**
```bash
freelog-cli dep update <resources...>[@version]
```

**简写命令**
```bash
freelog-cli update <resources...>[@version]
```

**示例**
```bash
# 更新单个依赖到最新版本
freelog-cli dep update resource-id

# 更新到指定版本
freelog-cli dep update resource-id@2.0.0

# 批量更新到最新版本
freelog-cli dep update resource-1 resource-2 resource-3

# 批量更新到指定版本
freelog-cli dep update resource-1@1.5.0 resource-2@2.0.0
```

**更新流程**
1. 检查当前版本
2. 查询目标版本可用性
3. 显示版本变更对比
4. 确认更新操作
5. 执行更新并更新配置文件

#### 3.5 查询依赖列表

**命令格式**
```bash
freelog-cli dep list [options]
```

**选项**
- `-v, --version <version>` - 指定版本号或 `latest`
- `--remote` - 查询线上版本（默认查询本地）
- `--auth` - 显示授权状态

**示例**
```bash
# 查询本地依赖列表
freelog-cli dep list

# 查询线上最新版本依赖
freelog-cli dep list -v latest --remote

# 查询指定版本依赖及授权状态
freelog-cli dep list -v 1.0.0 --remote --auth
```

**输出示例**
```
依赖列表 (v1.0.0):

┌──────────────┬─────────┬───────────┬──────────────┐
│ 名称         │ 版本    │ 授权状态  │ 签约策略     │
├──────────────┼─────────┼───────────┼──────────────┤
│ resource-1   │ 1.2.0   │ ✓ 已授权  │ 免费使用     │
│ resource-2   │ 2.0.1   │ ✗ 未授权  │ 按次付费     │
│ resource-3   │ 1.5.3   │ ✓ 已授权  │ 月度订阅     │
└──────────────┴─────────┴───────────┴──────────────┘
```

#### 3.6 同步依赖

**命令格式**
```bash
freelog-cli dep sync [options]
```

**选项**
- `-v, --version <version>` - 指定版本号或 `latest`
- `-f, --force` - 强制覆盖本地配置

**示例**
```bash
# 同步最新版本依赖
freelog-cli dep sync

# 同步指定版本依赖
freelog-cli dep sync -v 1.0.0

# 强制同步并覆盖本地配置
freelog-cli dep sync -v latest -f
```

**同步流程**
1. 从线上获取依赖列表
2. 对比本地配置差异
3. 显示变更内容
4. 确认同步操作
5. 更新本地配置文件

---

### 4. 信息同步

#### 4.1 初始化同步

**命令格式**
```bash
freelog-cli sync <resource>[@version]
```

**示例**
```bash
# 同步作品信息并初始化项目
freelog-cli sync resource-id

# 同步指定版本信息
freelog-cli sync resource-id@1.0.0

# 通过URL同步
freelog-cli sync https://freelog.com/resource/12345
```

**功能**: 拉取线上作品信息，创建或覆盖 `freelog.json` 文件

#### 4.2 同步作品信息

**命令格式**
```bash
freelog-cli sync work [options]
```

**选项**
- `-v, --version <version>` - 指定版本号
- `-f, --force` - 强制覆盖本地配置

**示例**
```bash
# 同步当前作品信息
freelog-cli sync work

# 同步指定版本作品信息
freelog-cli sync work -v 1.0.0
```

#### 4.3 同步所有信息

**命令格式**
```bash
freelog-cli sync -a [options]
freelog-cli sync --all [options]
```

**选项**
- `-v, --version <version>` - 指定版本号或 `latest`
- `-f, --force` - 强制覆盖本地配置

**示例**
```bash
# 同步最新版本所有信息
freelog-cli sync -a

# 同步指定版本所有信息
freelog-cli sync -a -v 1.0.0

# 强制同步所有信息
freelog-cli sync --all --version latest --force
```

**同步内容**:
- 作品基本信息
- 依赖列表
- 属性信息
- 配置信息
- 版本更新说明

#### 4.4 同步部分信息

**命令格式**
```bash
freelog-cli sync [options]
```

**选项**
- `-v, --version <version>` - 指定版本号
- `--props` - 仅同步属性信息
- `--config` - 仅同步配置信息
- `--changelog` - 仅同步更新说明

**示例**
```bash
# 同步属性信息
freelog-cli sync --props -v 1.0.0

# 同步配置信息
freelog-cli sync --config

# 同步更新说明
freelog-cli sync --changelog -v latest

# 组合同步
freelog-cli sync --props --config -v 1.0.0
```

---

### 5. 作品文件属性分析

#### 5.1 分析命令

**命令格式**
```bash
freelog-cli analyze [options]
```

**选项**
- `-f, --file <path>` - 指定文件路径
- `-o, --output <path>` - 输出分析结果到文件
- `--format <format>` - 输出格式 (json|table)

**示例**
```bash
# 根据配置文件分析
freelog-cli analyze

# 分析指定文件
freelog-cli analyze -f ./dist/bundle.zip

# 输出分析结果到文件
freelog-cli analyze -f ./dist/bundle.zip -o analysis.json

# 以表格形式显示
freelog-cli analyze --format table
```

#### 5.2 分析内容

分析工具会提取以下信息：
- 文件类型和格式
- 文件大小
- 文件结构
- 依赖关系
- 可配置项
- 元数据信息

#### 5.3 属性管理

分析完成后可以：
- 添加自定义属性
- 编辑识别的属性
- 验证属性格式
- 保存到配置文件

---

### 6. 配置文件结构

#### 6.1 freelog.json 文件

在项目根目录创建 `freelog.json` 配置文件

#### 6.2 配置文件结构

```json
{
  "version": "1.0.0",
  "type": "object",
  "local": {
    "buildDir": "./dist",
    "entryFile": "./dist/index.html",
    "excludes": ["node_modules", "*.log"]
  },
  "resource": {
    "resourceId": "",
    "resourceName": "",
    "resourceType": "",
    "coverImages": [],
    "description": "",
    "tags": []
  },
  "properties": [
    {
      "key": "theme",
      "name": "主题",
      "type": "select",
      "default": "light",
      "options": ["light", "dark"],
      "description": "应用主题颜色"
    }
  ],
  "customOptions": [
    {
      "key": "apiUrl",
      "name": "API地址",
      "type": "input",
      "default": "https://api.example.com",
      "description": "后端API服务地址"
    }
  ],
  "changelog": {
    "1.0.0": "初始版本发布",
    "1.1.0": "新增用户管理功能",
    "1.1.1": "修复登录问题"
  },
  "dependencies": [
    {
      "resourceId": "dep-resource-1",
      "name": "dependency-1",
      "version": "1.2.0",
      "versionRange": "^1.0.0",
      "policyId": "policy-123",
      "authStatus": true
    }
  ]
}
```

#### 6.3 配置字段说明

**local（本地配置）**
- `buildDir` - 构建输出目录
- `entryFile` - 入口文件路径
- `excludes` - 排除文件/目录列表

**resource（作品信息）**
- `resourceId` - 作品唯一标识
- `resourceName` - 作品名称
- `resourceType` - 作品类型
- `coverImages` - 封面图片列表
- `description` - 作品描述
- `tags` - 标签列表

**properties（属性信息）**
- `key` - 属性键名
- `name` - 属性显示名称
- `type` - 属性类型（input/select/radio/checkbox等）
- `default` - 默认值
- `options` - 可选项（select类型）
- `description` - 属性描述

**customOptions（自定义配置）**
- 结构同 properties
- 用于运行时可配置项

**changelog（更新说明）**
- 键为版本号
- 值为版本更新说明

**dependencies（依赖信息）**
- `resourceId` - 依赖资源ID
- `name` - 依赖名称
- `version` - 当前使用版本
- `versionRange` - 版本范围
- `policyId` - 策略ID
- `authStatus` - 授权状态

---

### 7. 项目初始化

#### 7.1 创建新项目

**命令格式**
```bash
freelog-cli init [project-name] [options]
```

**选项**
- `-t, --template <template>` - 指定模板名称
- `--list` - 列出所有可用模板
- `-f, --force` - 强制覆盖已存在的目录

**示例**
```bash
# 交互式创建项目
freelog-cli init

# 指定项目名称
freelog-cli init my-project

# 使用指定模板
freelog-cli init my-project -t vite-react

# 列出所有模板
freelog-cli init --list
```

#### 7.2 可用模板

根据项目结构，提供以下模板：

**JavaScript 包模板**
- `package-js` - 纯 JavaScript 包
- `package-react` - React 组件库
- `package-vue` - Vue 组件库

**Vite 项目模板**
- `vite-react` - Vite + React
- `vite-react-ts` - Vite + React + TypeScript
- `vite-vue` - Vite + Vue
- `vite-vue-ts` - Vite + Vue + TypeScript

**Webpack 项目模板**
- `webpack-react` - Webpack + React
- `webpack-react-ts` - Webpack + React + TypeScript
- `webpack-vue` - Webpack + Vue
- `webpack-vue-ts` - Webpack + Vue + TypeScript

#### 7.3 初始化流程

1. 询问项目名称（如未指定）
2. 选择项目模板
3. 配置项目选项
4. 创建项目目录
5. 复制模板文件
6. 初始化 Git 仓库
7. 安装依赖包
8. 显示后续步骤

---

### 8. 帮助文档

#### 8.1 全局帮助

```bash
freelog-cli --help
# 或
freelog-cli -h
```

**输出示例**
```
Usage: freelog-cli <command> [options]

Freelog CLI - 作品开发与发布工具

Commands:
  init [name]           初始化新项目
  login                 用户登录
  logout                用户登出
  publish               发布作品
  add <resource>        添加依赖
  change <resource>     修改依赖
  remove <resources...> 删除依赖
  update <resources...> 更新依赖
  dep <command>         依赖管理命令
  sync [resource]       同步信息
  analyze               分析文件属性

Options:
  -v, --version         显示版本号
  -h, --help            显示帮助信息

Run 'freelog-cli <command> --help' for more information on a command.
```

#### 8.2 命令帮助

每个命令都支持 `--help` 选项：

```bash
freelog-cli login --help
freelog-cli publish --help
freelog-cli add --help
freelog-cli dep --help
freelog-cli sync --help
```

**命令帮助示例**
```bash
freelog-cli publish --help
```

**输出**
```
Usage: freelog-cli publish [options]

发布作品到 Freelog 平台

Options:
  -gu, --global-user        使用全局登录用户
  -wu, --workspace-user     使用工作空间登录用户
  -d, --draft               发布为草稿
  -f, --file <path>         指定作品文件路径
  -c, --config <path>       指定配置文件路径
  -m, --message <message>   版本更新说明
  --major                   主版本号递增
  --minor                   次版本号递增
  --patch                   补丁版本号递增
  -h, --help                显示帮助信息

Examples:
  $ freelog-cli publish
  $ freelog-cli publish -gu
  $ freelog-cli publish -d
  $ freelog-cli publish -f ./dist/bundle.zip
  $ freelog-cli publish --patch -m "修复bug"
```

---

## 命令参考

### 快速命令索引

| 命令 | 说明 | 示例 |
|------|------|------|
| `init` | 初始化项目 | `freelog-cli init my-project` |
| `login` | 用户登录 | `freelog-cli login -g` |
| `logout` | 用户登出 | `freelog-cli logout` |
| `publish` | 发布作品 | `freelog-cli publish -gu` |
| `add` | 添加依赖 | `freelog-cli add resource-id@1.0.0` |
| `change` | 修改依赖 | `freelog-cli change resource-id` |
| `remove` | 删除依赖 | `freelog-cli remove resource-id` |
| `update` | 更新依赖 | `freelog-cli update resource-id` |
| `dep list` | 查询依赖 | `freelog-cli dep list -v latest` |
| `dep sync` | 同步依赖 | `freelog-cli dep sync` |
| `sync` | 同步信息 | `freelog-cli sync -a` |
| `analyze` | 分析文件 | `freelog-cli analyze -f ./dist/bundle.zip` |

---

## 配置文件结构

### freelog.json 完整示例

```json
{
  "version": "1.0.0",
  "type": "object",
  "local": {
    "buildDir": "./dist",
    "entryFile": "./dist/index.html",
    "excludes": [
      "node_modules",
      "*.log",
      ".git",
      "*.tmp"
    ],
    "includes": [
      "dist/**/*",
      "public/**/*"
    ]
  },
  "resource": {
    "resourceId": "res_1234567890",
    "resourceName": "my-freelog-app",
    "resourceType": "widget",
    "coverImages": [
      "https://example.com/cover1.jpg",
      "https://example.com/cover2.jpg"
    ],
    "description": "这是一个 Freelog 应用示例",
    "tags": ["widget", "react", "vite"],
    "intro": "详细的应用介绍...",
    "category": "工具",
    "language": "zh-CN",
    "license": "MIT"
  },
  "properties": [
    {
      "key": "theme",
      "name": "主题",
      "type": "select",
      "default": "light",
      "required": true,
      "options": [
        { "value": "light", "label": "浅色主题" },
        { "value": "dark", "label": "深色主题" }
      ],
      "description": "应用主题颜色"
    },
    {
      "key": "title",
      "name": "标题",
      "type": "input",
      "default": "默认标题",
      "required": true,
      "maxLength": 50,
      "description": "应用标题文字"
    },
    {
      "key": "enableFeature",
      "name": "启用功能",
      "type": "checkbox",
      "default": false,
      "description": "是否启用高级功能"
    }
  ],
  "customOptions": [
    {
      "key": "apiUrl",
      "name": "API地址",
      "type": "input",
      "default": "https://api.example.com",
      "required": true,
      "pattern": "^https?://",
      "description": "后端API服务地址"
    },
    {
      "key": "pageSize",
      "name": "分页大小",
      "type": "number",
      "default": 20,
      "min": 10,
      "max": 100,
      "description": "列表分页每页显示数量"
    }
  ],
  "changelog": {
    "1.0.0": "初始版本发布",
    "1.0.1": "修复已知问题\n- 修复登录失败问题\n- 优化性能",
    "1.1.0": "新增功能\n- 新增用户管理\n- 新增数据导出功能",
    "1.1.1": "问题修复\n- 修复数据导出格式问题"
  },
  "dependencies": [
    {
      "resourceId": "dep_res_001",
      "name": "freelog-ui-library",
      "version": "1.2.0",
      "versionRange": "^1.0.0",
      "policyId": "policy_123456",
      "authStatus": true,
      "contractId": "contract_789012",
      "signDate": "2025-10-01T00:00:00Z"
    },
    {
      "resourceId": "dep_res_002",
      "name": "data-service",
      "version": "2.0.1",
      "versionRange": ">=2.0.0 <3.0.0",
      "policyId": "policy_234567",
      "authStatus": false,
      "required": true
    }
  ],
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "publish": "freelog-cli publish"
  },
  "meta": {
    "createdAt": "2025-10-01T00:00:00Z",
    "updatedAt": "2025-10-30T00:00:00Z",
    "author": "developer@example.com",
    "contributors": [
      "developer1@example.com",
      "developer2@example.com"
    ]
  }
}
```

---

## 错误处理

### 错误代码规范

| 错误代码 | 说明 | 解决方案 |
|----------|------|----------|
| `AUTH_001` | 未登录或登录已过期 | 请执行 `freelog-cli login` 重新登录 |
| `AUTH_002` | 登录凭证无效 | 请检查用户名和密码 |
| `AUTH_003` | 权限不足 | 请联系管理员授权 |
| `FILE_001` | 文件不存在 | 请检查文件路径 |
| `FILE_002` | 文件格式不支持 | 请使用支持的文件格式 |
| `FILE_003` | 文件大小超限 | 请减小文件大小 |
| `VERSION_001` | 版本号格式错误 | 请使用语义化版本号（如 1.0.0） |
| `VERSION_002` | 版本号已存在 | 请使用新的版本号 |
| `VERSION_003` | 版本号不能降级 | 版本号必须大于当前版本 |
| `DEP_001` | 依赖不存在 | 请检查依赖ID或名称 |
| `DEP_002` | 依赖版本不存在 | 请检查版本号 |
| `DEP_003` | 依赖未授权 | 请完成依赖授权流程 |
| `CONFIG_001` | 配置文件不存在 | 请执行 `freelog-cli sync` 初始化配置 |
| `CONFIG_002` | 配置文件格式错误 | 请检查 JSON 格式 |
| `NETWORK_001` | 网络连接失败 | 请检查网络连接 |
| `NETWORK_002` | 服务器响应超时 | 请稍后重试 |
| `SERVER_001` | 服务器内部错误 | 请联系技术支持 |

### 常见错误场景

#### 1. 登录相关

**错误**: 登录失败
```bash
Error [AUTH_002]: 登录凭证无效
  用户名或密码错误
  
解决方案:
  1. 检查用户名和密码是否正确
  2. 确认账户是否已激活
  3. 如忘记密码，请访问 https://freelog.com/reset-password
```

**错误**: Token 过期
```bash
Error [AUTH_001]: 登录已过期
  请重新登录
  
解决方案:
  $ freelog-cli login
```

#### 2. 发布相关

**错误**: 版本号错误
```bash
Error [VERSION_001]: 版本号格式错误
  当前版本号: 1.0
  
解决方案:
  版本号必须遵循语义化版本规范（如 1.0.0）
  $ freelog-cli publish --patch
```

**错误**: 文件不存在
```bash
Error [FILE_001]: 找不到构建文件
  路径: ./dist/bundle.zip
  
解决方案:
  1. 检查构建目录配置
  2. 确保已执行构建命令
  3. 检查 freelog.json 中的 buildDir 配置
```

#### 3. 依赖相关

**错误**: 依赖未授权
```bash
Error [DEP_003]: 依赖未授权
  依赖: data-service@2.0.1
  
解决方案:
  请完成依赖授权流程
  $ freelog-cli add data-service@2.0.1
```

**错误**: 依赖冲突
```bash
Warning [DEP_004]: 依赖版本冲突
  依赖: ui-library
  当前版本: 1.0.0
  需要版本: ^2.0.0
  
解决方案:
  $ freelog-cli update ui-library@2.0.0
```

#### 4. 配置文件相关

**错误**: 配置文件格式错误
```bash
Error [CONFIG_002]: 配置文件格式错误
  文件: freelog.json
  行: 15, 列: 8
  错误: Unexpected token }
  
解决方案:
  请检查 JSON 格式是否正确
  建议使用 JSON 校验工具
```

### 错误日志

错误日志保存位置：
- Windows: `%USERPROFILE%\.freelog-cli\logs\`
- macOS/Linux: `~/.freelog-cli/logs/`

查看最近的错误日志：
```bash
# 查看今日日志
cat ~/.freelog-cli/logs/$(date +%Y-%m-%d).log

# 查看最后 50 行日志
tail -n 50 ~/.freelog-cli/logs/$(date +%Y-%m-%d).log
```

---

## 开发计划

### Phase 1: 核心功能 (v1.0.0)

**优先级: 高**

- [x] 项目初始化 (`init`)
- [ ] 用户认证系统 (`login`, `logout`)
  - [ ] 全局登录
  - [ ] 工作空间登录
  - [ ] 登录状态管理
- [ ] 作品发布 (`publish`)
  - [ ] 草稿发布
  - [ ] 正式版本发布
  - [ ] 版本号管理
- [ ] 配置文件管理
  - [ ] 配置文件生成
  - [ ] 配置文件验证
  - [ ] 配置文件同步

### Phase 2: 依赖管理 (v1.1.0)

**优先级: 高**

- [ ] 依赖添加 (`add`)
- [ ] 依赖修改 (`change`)
- [ ] 依赖删除 (`remove`)
- [ ] 依赖更新 (`update`)
- [ ] 依赖查询 (`dep list`)
- [ ] 依赖同步 (`dep sync`)
- [ ] 策略签约流程
- [ ] 授权状态管理

### Phase 3: 信息同步 (v1.2.0)

**优先级: 中**

- [ ] 作品信息同步 (`sync`)
- [ ] 版本信息同步
- [ ] 属性信息同步
- [ ] 配置信息同步
- [ ] 更新说明同步
- [ ] 选择性同步功能

### Phase 4: 高级功能 (v1.3.0)

**优先级: 中**

- [ ] 文件属性分析 (`analyze`)
- [ ] 自定义属性管理
- [ ] 智能文件识别
- [ ] 元数据提取
- [ ] 批量操作支持
- [ ] 交互式向导改进

### Phase 5: 优化与增强 (v2.0.0)

**优先级: 低**

- [ ] 性能优化
  - [ ] 文件上传优化（分片、断点续传）
  - [ ] 并发请求优化
  - [ ] 缓存机制
- [ ] 用户体验改进
  - [ ] 更友好的错误提示
  - [ ] 进度条和状态显示
  - [ ] 彩色输出
  - [ ] 日志系统
- [ ] 扩展功能
  - [ ] 插件系统
  - [ ] 自定义模板
  - [ ] 多语言支持
  - [ ] GUI 界面（可选）
- [ ] CI/CD 集成
  - [ ] GitHub Actions 集成
  - [ ] GitLab CI 集成
  - [ ] Jenkins 集成

### 技术债务

- [ ] 单元测试覆盖率 > 80%
- [ ] E2E 测试
- [ ] 性能基准测试
- [ ] 安全性审计
- [ ] 文档完善
- [ ] TypeScript 类型定义

### 已知问题

1. 大文件上传可能超时
2. 网络不稳定时缺少重试机制
3. 配置文件格式错误提示不够友好
4. 依赖冲突检测不完善

---

## 附录

### A. 技术栈

- **语言**: Node.js
- **框架**: Commander.js（命令行框架）
- **依赖管理**: pnpm + monorepo (Lerna)
- **交互**: Inquirer.js
- **文件处理**: fs-extra, archiver
- **网络请求**: axios
- **日志**: winston
- **测试**: Jest

### B. 参考文档

- [Freelog 平台文档](https://freelog.com/docs)
- [Commander.js 文档](https://github.com/tj/commander.js)
- [Inquirer.js 文档](https://github.com/SBoudrias/Inquirer.js)
- [语义化版本规范](https://semver.org/lang/zh-CN/)

### C. 贡献指南

欢迎贡献代码和提出建议！

**贡献流程**:
1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

### D. 联系方式

- 技术支持: support@freelog.com
- Bug 报告: https://github.com/freelog/cli/issues
- 功能建议: https://github.com/freelog/cli/discussions

---

**最后更新**: 2025-10-30  
**文档版本**: v1.0  
**维护者**: Freelog Team
