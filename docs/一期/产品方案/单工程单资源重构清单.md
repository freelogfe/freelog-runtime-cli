# 单工程单资源重构清单

最后更新：2026-09-08。本清单是 `--file` → 唯一 `--artifact` 重构的唯一进度表；每一项必须先讲清设计和实现方案，得到复核结论后才进入实现。

## 协作规则

每个未完成项固定经过以下状态，不跨级：

```text
[ ] 待讲解 → [~] 已讲解，等待复核 → [>] 方案已确认，待实现
→ [#] 已实现，待验证 → [x] 已验证完成
```

每次讲解必须写明：用户场景、命令输入与输出、本地状态变化、平台调用边界、错误与恢复、测试方案、与普通资源/主题/插件的差异。复核发现问题时回到“待讲解”或“方案已确认”，不得直接修改代码。

## 已完成且已验证

- [x] 产品决定：一个工程只管理一个资源；详见 [08-单工程单资源与产物路径](./脚手架设计/ARCHITECTURE/08-单工程单资源与产物路径.md)。
- [x] CLI 顶层及共享参数删除 `--file`；`create`、`bind` 使用 `--artifact`。
- [x] 公开入口的 `create-version`、`update-version`、`version set` 使用 `--artifact`。
- [x] 新建身份固定为 `1.json`，第二份身份被拒绝。
- [x] 本轮静态验证：`typecheck`、`build`、104/104 单测通过。

## 必须逐项复核的后续工作

### A. 本地状态迁移与边界

- [ ] 讲解旧多身份工程的检测、报错和人工迁移路径：何时拒绝、如何拆分目录、怎样保留工作稿、`bind` 是否足够。
- [ ] 讲解并确认 `index.json` 的完全删除策略：新工程不写、旧工程如何识别、事务恢复是否仍需要它。
- [ ] 讲解并确认 `1.json.filePath` 的生命周期：init、create、bind、version set、发版成功与失败分别如何写或不写。
- [ ] 实现、测试、复核 A 三项。

### B. 命令逐条收敛

- [ ] `init`：普通资源与主题/插件的初始默认路径、非空目录、login 后 init 的例外。
- [ ] `create`：`--artifact` 只是默认路径记录，是否应要求路径存在、普通文件与主题/插件的形态校验。
- [ ] `bind`：已有线上资源接入时 `--artifact` 的必填规则、换绑、工作稿清理。
- [ ] `create-version`：未传 artifact 的默认路径、显式 artifact 的回写时机、首版 prepare 与提交。
- [ ] `update-version`：换文件/换构建目录、工作稿底版本、重新上传与路径回写。
- [ ] `version set`：只改默认路径的输入校验与错误恢复。
- [ ] `status`、`version`、属性、配置、依赖、策略、上下架：确认它们不再接受或依赖身份路径选择器。
- [ ] 每一条确认后实现并添加定向单测。

### C. 主题与插件完整路径

- [ ] 新模板工程：init → build → create → 首版发布。
- [ ] 已有本地主题/插件工程：create / bind → 首版或更新版。
- [ ] `dist` 改为 `build`，目录不存在、目录为空、工程根目录、zip 文件等边界。
- [ ] 与普通单文件资源的共同规则和唯一差异：产物形态与临时压缩。

### D. 设计与用户文档

- [ ] 先重写真实场景 S8、S28 为“旧多身份工程安全迁移”；补全保留工作稿、拆分目录、重新 bind 与失败恢复的用户旅程。
- [ ] 修正真实场景 S53：策略模板展示全部适用项，不按免费/付费筛选。
- [ ] 将场景实现 S1、S4、S25、S26、S37、S42、S56–S58 及其余 `--file` 示例逐条改为 `--artifact`，并逐条复核可执行性。
- [ ] 清理 PHASE、ARCHITECTURE、场景设计中所有多身份、`N.json` 编号、`index.json`、`--file` 和兼容写法。
- [ ] 精校使用手册的每个示例，确保只出现 `--artifact`，且能直接复制执行。
- [ ] 更新发布/测试文档，说明当前测试数、命令面和迁移提示。

### E. 端到端验证与交付

- [ ] 将 `test/` 的 dev 真网脚本全部从 `--file` 改为 `--artifact`。
- [ ] 使用 primary dev 账号分别真网验证普通文件首版/更新版、主题首版/更新版、bind、依赖、策略、上下架。
- [ ] 执行 `test`、`typecheck`、`build`、`verify:package`；记录命令、结果和任何未验证缺口。
- [ ] 最后从用户场景逐条复盘，确认不存在“一个工程多资源”或双路径参数残留。
