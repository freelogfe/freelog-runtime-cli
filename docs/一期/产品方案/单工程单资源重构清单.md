# 多资源本地状态重构清单

最后更新：2026-09-09。本清单是“多资源本地状态 + `--resource` 身份选择”与唯一 `--artifact` 产物路径重构的唯一进度表；每一项必须先讲清设计和实现方案，得到复核结论后才进入实现。

## 协作规则

每个未完成项固定经过以下状态，不跨级：

```text
[ ] 待讲解 → [~] 已讲解，等待复核 → [>] 方案已确认，待实现
→ [#] 已实现，待验证 → [x] 已验证完成
```

每次讲解必须写明：用户场景、命令输入与输出、本地状态变化、平台调用边界、错误与恢复、测试方案、与普通资源/主题/插件的差异。复核发现问题时回到“待讲解”或“方案已确认”，不得直接修改代码。

## 已完成且已验证

- [>] 产品决定：一个工程可管理多份独立单资源；详见 [08-多资源本地状态、选择与产物路径](./脚手架设计/ARCHITECTURE/08-多资源本地状态、选择与产物路径.md)。
- [>] 公开命令使用 `--resource` 选择身份，`create`、`bind` 使用 `--artifact` 指定产物。
- [x] 公开入口的 `create-version`、`update-version`、`version set` 使用 `--artifact`。
- [>] 旧实现“新建身份固定为 `1.json`、第二份身份被拒绝”已被新决定取代：新身份使用未占用的 `N.json`，第二份及后续身份允许创建。
- [x] 本轮静态验证：`typecheck`、`build`、`verify:package`、114 项单测通过；这不是多资源命令路由的验收。

## 必须逐项复核的后续工作

### A. 本地状态、选择与冲突恢复

- [>] 已讲解并确认多资源状态、`--resource` 选择、标题字段与已有状态冲突恢复；按 [08](./脚手架设计/ARCHITECTURE/08-多资源本地状态、选择与产物路径.md) 实现。
- [x] 实现 `N.json` / `N.version.json` / `index.json` 的多资源状态接口与事务恢复。
- [#] 统一身份选择、TTY 列表与非交互拒绝已做初版实现；经全盘审计发现策略命令和创建/接入生命周期存在路由缺口，不能标记完成。
- [#] `update --title` 回写与 `resource sync` 已做初版实现；全量工作稿完整性检查、显式选择器的环境校验和旧状态标题迁移口径待补。
- [#] A 已完成局部单测、类型检查、构建与打包验证；多资源路由测试、状态完整性测试和 dev 真网验证均未完成。

### B. 命令逐条收敛

- [ ] `init`：普通资源与主题/插件的初始默认路径、非空目录、login 后 init 的例外。
- [ ] `create`：`--artifact` 只是默认路径记录，是否应要求路径存在、普通文件与主题/插件的形态校验。
- [ ] `bind`：已有线上资源接入时 `--artifact` 的必填规则、换绑、工作稿清理。
- [ ] `create-version`：未传 artifact 的默认路径、显式 artifact 的回写时机、首版 prepare 与提交。
- [ ] `update-version`：换文件/换构建目录、工作稿底版本、重新上传与路径回写。
- [ ] `version set`：只改默认路径的输入校验与错误恢复。
- [ ] `status`、`version`、属性、配置、依赖、策略、上下架：确认它们不再接受或依赖身份路径选择器。
- [ ] 每一条确认后实现并添加定向单测。

### C. 主题与插件完整路径

- [ ] 新模板工程：init → build → create → 首版发布。
- [ ] 已有本地主题/插件工程：create / bind → 首版或更新版。
- [ ] `dist` 改为 `build`，目录不存在、目录为空、工程根目录、zip 文件等边界。
- [ ] 与普通单文件资源的共同规则和唯一差异：产物形态与临时压缩。

### D. 设计与用户文档

- [x] 重写 ARCHITECTURE 02/04/05/06：统一多资源 `N.json`、`--resource`、`--artifact`、标题与 `resource sync` 的边界。
- [x] 逐章修订 PHASE：所有身份相关命令使用 `--resource` 或 08 的零/一/多状态选择；删除所有 `--file` 兼容分支。
- [x] 重写真实场景 S8、S28：改为同工程多资源的选择、冲突恢复与完整状态单元删除/重新 bind；补全保留工作稿的用户旅程。
- [#] 新增真实场景：零状态的新建/接入指引、单状态静默选择、多状态 TTY 选择与非交互拒绝、标题歧义、`resource sync` 的全量/部分失败，以及多资源工程中的新增/接入路由；待与代码和 CLI 级测试核对后再验收。
- [x] 将这些场景同步到场景实现：每条命令用 `--resource` 选择身份、用 `--artifact` 传产物，并标注可执行验证。
- [ ] 修正真实场景 S53：策略模板展示全部适用项，不按免费/付费筛选。
- [x] 已核查并清理 PHASE、ARCHITECTURE、场景设计中的有效“单工程唯一身份”与 `--file` 兼容语义；历史/迁移说明仅保留为明确的禁用提示，多身份 `N.json` / `index.json` 规则以 08 为准。
- [ ] 精校使用手册的每个示例，确保身份只用 `--resource`（或单状态省略），产物只用 `--artifact`，且能直接复制执行。
- [ ] 更新发布/测试文档，说明当前测试数、命令面和迁移提示。

### E. 端到端验证与交付

- [x] 已完成 `test/` dev 真网脚本迁移：产物参数使用 `--artifact`，换绑目标使用显式 `--resource`；Node 语法检查与 `run-all-scenarios` / `verify-commands` / `verify-scenarios` dev 真网回归通过。
- [#] primary dev 已验证普通文件首版/更新版、主题首版/更新版、bind、依赖、策略、上下架；多状态 TTY 选择仍待真网验证。
- [x] 已执行 `test`、`typecheck`、`build`、`verify:package`；当前记录为 116 项单测通过。
- [ ] 最后从用户场景逐条复盘，确认不存在“一个工程多资源”或双路径参数残留。

### F. 多资源命令路由与状态完整性审计

本节来自 2026-09-09 的全盘静态审计。只有以下各项都验证通过，A 才能从 `[#]` 变为 `[x]`。

- [>] 已确认路由原则：常规身份命令先解析一份资源；`resource sync` 是唯一可在省略选择器时批量处理当前环境全部身份的命令；`create` / `bind` 是创建状态单元的专属生命周期入口，不能套用常规“多份即选择”钩子。
- [#] `policy apply`、`policy set` 已统一读取 `--resource`，并已覆盖显式选择器的命令层转交；待补 TTY 回填及根命令的 `--yes` 非交互拒绝测试。
- [#] 已实现“显式复用既有状态 / 默认新增状态 / 唯一未绑定状态便捷接续”的 create/bind 路由；已覆盖 bind 多状态新增与接续的领域测试，待补 `--force` 及 CLI 级测试。
- [#] 已在解析器、create、bind 与 `resource sync` 前增加全量状态单元预检：解析所有 `N.json`、查重复 ID/标识符/规范化路径、枚举所有 `N.version.json` 并验证同号身份、资源 ID 与类型；待补每一类命令的 CLI 级与真网证据。
- [#] `resource sync --resource` 已拒绝非当前 `--env` 的身份；旧状态没有标题时仍显示“未同步标题”，成功同步后补齐。待补旧状态迁移与 CLI 输出测试。
- [ ] 按 [08 的命令路由矩阵](./脚手架设计/ARCHITECTURE/08-多资源本地状态、选择与产物路径.md#31-命令路由矩阵) 为每类命令补 CLI 级测试：零/一/多、TTY 选择与退出、非 TTY/`--yes`、显式选择器、冲突/孤儿稿、普通文件/主题/插件。
- [ ] 每一组修复后重跑单测、类型检查、构建、打包；涉及平台的写命令再跑 dev 真网场景并记录结果。
