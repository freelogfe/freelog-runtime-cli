# 合集功能拓扑与证据驱动测试门禁

状态：**实施与 dev 验收进行中，2026-09-18。** 本文仍是功能点、证据与剩余门禁的唯一索引。

本文把合集的用户能力拆成有向依赖图，并为每个节点和每条写入边定义可证明的测试事实。它不是“跑过几条 happy path 就算完成”的清单：只有所有声明功能点均有来源证据、自动化断言和真实 dev 读回证据，才可把合集全流程称为通过。

“100%”在本文中有严格含义：**100% 已声明的节点、边、失败分支和不变量均被逐项覆盖并有证据**。没有任何测试方法能证明“绝对不存在所有未知问题”；因此未能从 tools-lib、Console 可执行代码或 dev 响应确定的契约一律标为 `BLOCKED`，不得用猜测实现后再补测试。

## 1. 证据等级与放行规则

| 等级 | 含义 | 可用于什么 | 不可用于什么 |
|---|---|---|---|
| E1 | 正式设计与可信业务梳理一致 | 列功能边界、生成测试用例 | 推断请求 payload |
| E2 | 当前 `packages/tools-lib` 存在 Node 端请求包装 | 锁定方法、URL、已声明 DTO 字段 | 断言服务端实际响应或未声明字段 |
| E3 | Console 当前可执行代码确认调用/映射 | 核对 UI 行为、payload 映射与分支 | 覆盖 Console 自身未处理的异常 |
| E4 | CLI 单元/集成测试断言 | 证明本地实现的确定性行为 | 证明真实平台行为 |
| E5 | dev 真实请求和完整读回 | 证明当前环境端到端契约 | 推广到 prod 或未测资源类型 |

一个**已实现**功能点达到可交付最低门槛必须同时有 E1、E2、E4、E5；若涉及 Console 专有映射，还必须有 E3。纯只读诊断可省 E5，但仍需在 dev 至少验证一次真实响应形状。任何远端写入若不能给出唯一的读回判定，则不能进入 dev 写入阶段。

## 2. 用户能力拓扑

```text
登录态 + 环境
   │
   ├─ 解析/重读本人合集目标 ───────────────┐
   │                                      │
   ├─ 创建合集壳 / 接入已有合集            │
   │                                      ▼
   │                              合集当前事实
   │                       (owner / subjectType / status / RSS)
   │                                      │
   │          ┌───────────────────────────┼─────────────────────────┐
   │          ▼                           ▼                         ▼
   │   服务端目录草稿                 listing / 收录规则         合集表单操作草稿
   │   (单品、排序、授权)                                        (属性/配置/描述/展示/依赖)
   │          │                           │                         │
   │          │                           └──────────┐              │
   │          │                                      │              │
   │          └─────── 目录差异 / 自动模式 ─────────┼──────────────┤
   │                                                 ▼              ▼
   │                                      collection publish（唯一合并点）
   │                                                 │
   │                                    已发布合集快照 + 已发布目录
   │                                                 │
   └──────────────────── 合集自身策略 ──────────────┼──────────────┘
                                                     ▼
                                          online / offline（独立状态）
```

### 2.1 不可跨越的边界

1. 单品目录草稿、listing、收录规则、合集表单、策略、上下架是不同的远端事实；一个命令不得顺带写另一事实。
2. `collection publish` 是唯一把完整表单与可选目录草稿合并为已发布合集快照的写点；它不创建单资源版本，也不隐式上架。
3. 单品上游授权、合集直接依赖和合集自身策略三者只可复用选择/签约交互，不能共用存储字段或发布接口。
4. `.freelog/N.json` 只保存主体身份；`subject:"collection"` 不含 `filePath`。表单操作草稿和发布 intent 是可回收事务数据，不能变成第二份线上目录事实。
5. 标题是可变、可重复的展示值，永远不能用作目标、单品、策略或恢复匹配键。

## 3. 功能点总表（覆盖范围的唯一索引）

状态列：`已实现待 dev`、`待实现`、`BLOCKED`。每一行都必须在第 5 节有至少一个同 ID 测试用例；新增命令或接口时先新增本表行，再写设计/代码。

| ID | 功能点 | 前置拓扑节点 | 远端写入 | 当前状态 | 主要证据 |
|---|---|---|---|---|---|
| C-01 | 登录态与 `--env` 一致性 | 无 | 无 | 已实现，dev 正向已确认；环境错配待专项 | ARCHITECTURE/07、CLI auth/env |
| C-02 | 解析线上本人 `subjectType=4` 合集 | C-01 | 无 | 已实现，dev 以 ID 重读确认 | `domain/collection/target.ts` |
| C-03 | 创建合集壳、类型、标题、授权标识 | C-01 | `Resource.create` | 已实现，dev 直接叶子创建已确认 | 创建 Step1、`create.ts` |
| C-04 | bind 已有线上合集 | C-02 | 本地身份写入 | 已实现，dev 首次/重复 bind 已确认 | `bind.ts` |
| C-05 | 读取目录草稿（分页/搜索/排序） | C-02 | 无 | 已实现，dev 基础读取/`sortId` 已确认；>100 待样本 | `getCollectionItems_Draft` |
| C-06 | 添加本人已上架单资源 | C-02、C-05 | `addResourceItems_Draft` | 已实现，dev 添加与读回已确认 | 单品/01、`items.ts` |
| C-07 | 单品上游授权：已有合约/显式策略签约 | C-06 | `batchCreateContracts`、目录草稿 | 已实现；上游样本 dev 待补 | 单品/01、`contracts.ts` |
| C-08 | 改标题、移除、移动、重排目录草稿 | C-05 | 四种目录草稿写 | 已实现，dev rename/remove/move/标题降序已确认；其余排序字段待补 | 单品/03、`items.ts` |
| C-09 | 查询已有单品授权状态 | C-05 | 无 | 已实现，dev 已确认 | `getCollectionItemsAuth_Draft` |
| C-10 | 重新处理已有单品授权排除项 | C-09 | `updateCollectionItemAuthorization_Draft` | BLOCKED | tools-lib DTO 缺 body，未 dev 验证 |
| C-11 | 更新 listing：标题/封面/简介/标签 | C-02 | `Resource.update`、图片上传 | 已实现，dev 组合更新和原图封面读回已确认；边界待补 | 信息/01 |
| C-12 | 读取/保存完结状态与收录规则 | C-02、C-05 | `setCollectRules` | 已实现、dev 已确认 null 初始响应 | 信息/01、单品/02 |
| C-13 | 自动/手工目录模式切换 | C-05、C-12、已发布目录 | 规则写入 | 已实现、dev 已确认 on/off；全量多页待测 | 单品/02 |
| C-14 | 拉取/编辑/展示合集表单操作草稿 | C-02 | 仅本地草稿 | 已实现、dev 已确认 | 表单/01 |
| C-15 | 合集属性、可选配置、描述、展示设置 | C-14、实时类型能力 | 仅 publish 时 | 已实现；dev 已确认属性、文本/下拉配置、描述和展示；边界待补 | 表单/01、字段对照表 |
| C-16 | 合集直接依赖和签约 | C-14 | 合同 + 本地草稿 | 已实现、dev 已确认免费签约与读回 | 表单/02 |
| C-17 | 合集自身策略创建、列表、启停 | C-02 | `Resource.update` | BLOCKED | 策略后端契约未恢复 |
| C-18 | 发布表单和可选目录 merge | C-05、C-14、C-16 | `updateCollection` | 已实现，dev 已确认表单、目录与无表单稿的仅目录发布读回 | 发布/01 |
| C-19 | 发布未知结果的只读恢复 | C-18 | 无（只读） | 已实现 intent/resume；未知 POST 的 dev 可控样本待补 | 发布/01 |
| C-20 | 合集上架/下架 | C-18、C-17 | `Resource.update({status})` | BLOCKED | 依赖 C-17 后端恢复 |
| C-21 | 发行后重复管理与并发保护 | C-05~C-20 | 依具体命令 | 已实现命令级实时重读；多进程/dev 并发待测 | 所有管理页 |
| C-22 | CLI/Console 表单草稿隔离 | C-14 | 无（CLI 禁止草稿 API） | 已实现、静态测试待补 | 表单/01 |
| C-23 | 合集更新日志只读查询 | C-18 | 无 | 已实现、dev 已读回 | `getCollectionUpdateLogs`、维护/01 |
| C-24 | 合集授权合约只读查询 | C-02 | 无 | 已实现，空集合 dev 已读回 | 业务梳理 C3、`Contract.contracts`、维护/01 |
| C-25 | RSS 来源接入、同步与进度管理 | C-02 | RSS 写入 | **暂时忽略（OUT_OF_SCOPE）** | RSS/01；仅保留普通命令防误写 |

## 4. 实施前必须先关闭的契约缺口

以下项不是“以后再优化”，而是相应功能进入代码测试、dev 写入前的硬阻塞。验证方法只允许读取 tools-lib、Console 可执行代码或用**隔离 dev 测试资源**发一次最小请求；未经确认不得猜 payload。

| 阻塞 ID | 阻塞范围 | 当前已知事实 | 必须补的证据 | 解除条件 |
|---|---|---|---|---|
| B-01 | C-10 | `updateCollectionItemAuthorization_Draft` 仅声明 `resourceId/itemId`，真实 body 注释掉 | Console 授权处理器实际 body、写后目录/授权读回 | E3 + 一组 E5 成功/失败读回，且 contractId 映射唯一 |
| B-02 | C-13 | 自动开启要求目录草稿与已发布目录无差异 | `getCollectionItems` 与 draft 的全量响应形状、自动开启后目录语义 | 完整比较可重复；自动 on/off 各有 E5 |
| B-03 | C-14~C-19 | tools-lib `updateCollection.catalogueProperty` 少声明两个 Console 使用字段 | **已补齐两个枚举并由单测/发布 payload 覆盖；属性/可选配置的 dev 样本仍待补** | display 枚举已对齐；其余按 C-15 E5 执行 |
| B-04 | C-16/C-18 | 直接依赖的已有 `authExcludedItems` 不能由目录授权布尔值重建 | `batchContracts`、已发布表单快照与发布读回能否重建 exact contractId 排除映射 | 已有依赖/上抛样本 E5 可无损 pull→publish |
| B-05 | C-17/C-20 | 策略模板筛选、事件 DSL/编译契约仍不稳定 | 后端恢复后的模板、编译、创建、读回结果 | 文字策略和模板策略均 E5 成功；失败能分类 |

## 5. 测试用例矩阵

每个用例至少写入：测试时间、环境、账号别名、输入、请求摘要、远端读回、清理/保留决定。真实 token、cookie、密码和完整私有资源数据不得写入仓库或输出。

### 5.1 基础主体与定位（C-01~C-04）

| 用例 | 操作 | 必须断言的事实 |
|---|---|---|
| T-01 | 未登录或登录环境与 `--env` 不一致执行合集只读/写命令 | 在任意资源请求前失败；无本地身份写入、无远端写入 |
| T-02 | 以 `id:`、完整 `name:`、`file:N.json` 分别定位同一合集 | 三者均重读同一 resourceId/owner/subjectType；title 不参与定位 |
| T-03 | 零份和多份本地合集身份时省略 `--resource` | 零份要求线上稳定选择器；多份拒绝猜测；恰一份才可默认 |
| T-04 | 目标为他人资源、单资源、冻结合集、跨环境缓存 | 分别在对应门禁停止；不能仅信任 N.json |
| T-05 | 创建：层级选择、搜索、直接 leaf code | 最终都复验启用的 `subjectType=4` 叶子；请求固定 `subjectType:4` |
| T-06 | 创建：空/超长标题、完整 name、非法 name、名称已占、同名本人合集 | 不发 create；同名本人合集明确指向 bind |
| T-07 | create 远端成功后本地身份写入；本地写入失败模拟 | 成功 N.json 无 filePath；本地收尾未知时绝不重发 create |
| T-08 | bind 首次、重复、同 ID 跨环境、同 name 不同 ID | 首次写身份、重复幂等；冲突不覆盖原身份 |

### 5.2 服务端目录草稿与授权（C-05~C-10）

| 用例 | 操作 | 必须断言的事实 |
|---|---|---|
| T-09 | 草稿目录 >100 项分页读取、搜索、`sortId` 排序 | 所有页被读取；任一页异常则整体失败，不将首屏当完整事实 |
| T-10 | 添加一项：本人、上架、已有 latestVersion 的单资源 | 只调用目录草稿新增；读回存在 resourceId；未发布目录不变 |
| T-11 | 添加拒绝：他人、subjectType≠1、未上架、无版本、已在草稿 | 每项写前失败/未变更；无不合格资源进入 add payload |
| T-12 | 多项添加包含重复、已存在、有效项 | 稳定去重；报告 added/unchanged；不把忽略项说成成功 |
| T-13 | 单品没有上抛、已有上游合约、缺免费合约、缺付费合约 | 无上抛写空排除项；已有合约不重签；缺合约必须显式 policy；签后写 exact contractId；付费提示浏览器、不伪造支付 |
| T-14 | `--policy` 缺失、策略不属于上游、策略停用、签约读回没有 contractId | 在目录写前失败；绝不以 policyId 填 `excludedValue` |
| T-15 | 自动收录、RSS、冻结状态下 add/rename/remove/move/sort | 写请求数为 0；list/auth status 仍为只读可用 |
| T-16 | rename 空标题/不存在 itemId/成功读回 | 空或不存在不写；成功只改 itemTitle，不改来源资源标题 |
| T-17 | remove 多 itemId、其中不存在、并发已删除 | 要么整体在写前失败，要么按已设计的逐项结果模型报告；不可把部分成功称全成功 |
| T-18 | move before/after、同一锚点、跨页锚点、并发顺序改变 | targetSortId 根据全量 `sortId` 算出；读回相对顺序符合请求，否则未知 |
| T-19 | sort 三字段和两方向 | `reorder` 只发送 tools-lib 声明字段/方向；列表端统一按 `sortId` 全量读回已固化的目标顺序，绝不把 `itemTitle` / `resourceUpdateDate` 误传给列表接口 |
| T-20 | auth status 的 true/false/缺少 itemId/响应异常 | 只读输出稳定 itemId；false 不触发删除或改授权 |
| T-21 | 已有单品授权重新处理 | **仅 B-01 解除后执行**；验证原排除项不丢失、真实 contractId、失败不覆盖 |

### 5.3 listing、收录规则与 RSS 保护（C-11~C-13）

| 用例 | 操作 | 必须断言的事实 |
|---|---|---|
| T-22 | 只更新 title/intro/tags/cover 与多个字段组合 | 一次 `Resource.update` 只带显式 listing 字段，不带 status/目录/表单/策略；读回逐字段相等 |
| T-23 | listing 边界：标题100、简介200、20 标签/每项20、空/重复标签、空封面 | 本地拒绝不合法输入；`intro:""` 是清空；未传不改；封面空参数不清空 |
| T-24 | 封面 jpg/jpeg/png/静态 gif、动画 gif、签名不符、>5MiB、目录/工程外路径 | 仅合格图上传；每次告知“不裁剪、上传原图”；上传 URL 才进入 coverImages |
| T-25 | rules get：无规则/手工/自动/不完整响应 | 输出真实状态；不把 absence 当自动关闭的可写依据 |
| T-26 | `serialize` 独立更新、`auto off` | 保留未显式字段和已有 filterConditions；写后 get 结构化确认 |
| T-27 | `auto all/any` 缺 rules、空 rules、非法 field/operator/value、非 leaf type | 在规则写前失败 |
| T-28 | `authIdentity STARTS_WITH` | 发送 `<当前用户名>/<输入值>`；其他 operator 不加前缀；dev 验证实际匹配 |
| T-29 | 手工目录未发布差异时开启自动 | 拒绝并指向先 publish；不得逐项 delete 伪造 discard |
| T-30 | 自动状态下目录写、publish include-items yes/no/auto、关闭自动 | 完全遵守单品/02 的模式矩阵；关闭后读回 status=0 才解锁 |
| T-31 | RSS 合集 listing/规则/目录/表单操作 | 当前所有 RSS 写操作均拒绝；受控写请求数为 0 |

### 5.4 表单、直接依赖与发布（C-14~C-19）

| 用例 | 操作 | 必须断言的事实 |
|---|---|---|
| T-32 | 新合集首次空基线、已发布基线、存在服务端 form draft | 基线来源可辨；不存在版本不当成错误；远端草稿 fingerprint 被保存 |
| T-33 | form edit/show 与本地中断恢复 | edit 不调用 `updateCollection`；不改变目录/listing/策略/上架；本地操作草稿不污染 N.json |
| T-34 | 服务端 form draft 并发变化、未知 descriptor、类型配置读取失败 | 不做字段级猜测合并；停止发布，保留可诊断草稿 |
| T-35 | 属性、文本配置、下拉配置全部边界 | 严格执行字段对照表；可选配置只在当前合集叶子类型支持时可写；未知 radio/checkbox 拒绝发布 |
| T-36 | 六项 display 映射与完整 payload | 外部 JSON 映射完整 Console 枚举；所有 display 字段必填，不能省略后误清空 |
| T-37 | 直接依赖：默认范围、范围不命中、环、自己、非单资源、冻结/RSS | 不写表单草稿或签约；licensee 必为合集 resourceId |
| T-38 | 直接依赖：已有合约、免费签约、付费签约与中断恢复 | 合同查询/签约均以 collection 为 licensee；签约意图先落盘；付费不阻断但不称支付完成 |
| T-39 | 直接依赖上抛与已有 authExcludedItems | **仅 B-04 解除后执行**；pull→edit→publish 不得丢失 exact mapping |
| T-40 | publish：无操作草稿、首次空基线、已有快照、目录相同/不同、include auto/yes/no | 全量读取 published/draft 目录和表单；只在允许时 merge；不创建版本、不上架 |
| T-41 | publish payload 完整性 | inputAttrs、descriptors、description、catalogueProperty、dependencies、authExcludedItems、merge flag 均可由基线+草稿复现；不夹带 listing/规则/策略/status |
| T-42 | publish 远端明确失败、超时/中断、成功后本地清理失败 | `prepared→sending→confirmed/rejected/unknown` 可审计；只读 resume；未知不重发 |
| T-43 | publish 读回与并发差异 | 仅“已发布快照和要求 merge 的目录都等于 intent”可 confirmed；其它差异保持 unknown |

### 5.5 策略、上架与发行后管理（C-17、C-20、C-21）

| 用例 | 操作 | 必须断言的事实 |
|---|---|---|
| T-44 | policy list / template list 分页 | 列合集自身策略；模板请求全部、CLI 内每页20；不以单品策略替代 |
| T-45 | 文本策略、模板策略、编译类型、策略启停 | **仅 B-05 解除后执行**；模板 compileType 透传，原始文本默认 collection，读回 policyId |
| T-46 | online：未发布、无策略、全停用、已有启用策略、已上架 | 仅已发布且有启用合集自身策略可 status=1；不自动 publish/create/enable policy |
| T-47 | offline：已下架、已上架、冻结/无权 | 已下架幂等；仅写 status=4；不删除任何目录/合同/草稿/身份 |
| T-48 | 发行后重复执行全部已实现管理命令 | title 缓存不作为事实；每次重读 owner/主体/冻结/RSS/自动状态；没有命令跨越第2节边界 |
| T-49 | CLI 表单流程的草稿 API 隔离 | `form pull/edit/show/publish` 的请求列表中没有 `lookDraft`、`saveVersionsDraft`、`deleteResourceDraft`；仅已发布快照指纹参与并发门禁 |
| T-50 | 更新日志分页、空日志、不同操作类型 | 仅 `getCollectionUpdateLogs`；按稳定日志 ID/时间显示，绝不把日志当发布成功或恢复依据 |
| T-51 | 合约列表：无合约、免费、待支付、已失效/异常状态 | 仅以合集 resourceId 为查询主体；不改变合同、依赖或目录，状态文案不把签约等同支付 |
| T-52 | RSS 已存在目标的识别与保护 | 当前仅做保护性测试：普通目录、表单、依赖、规则、发布和 listing 写命令拒绝写入；不测试或注册任何 RSS 专属能力 |

## 6. 自动化与 dev 的执行顺序

拓扑顺序是强制的：下游测试只能使用已由上游读回确认的事实，不能把 mock 成功当作 dev 前置条件。

1. **静态证据审查**：逐项关闭 B-01~B-06 或标记该功能不实施；核对所有命令、DTO、设计链接和功能点 ID。
2. **领域单元测试**：先覆盖 T-01~T-20 当前已实现能力，使用注入的 tools-lib adapter，逐个断言“失败前零写入”“精确 payload”“写后读回”。
3. **命令/架构测试**：help、参数互斥、`--resource` 主体隔离、无 `artifact:` 合集选择器、命令不直接打平台、单资源解析器拒绝合集身份。
4. **未实现能力的实现测试**：每完成 C-11~C-21 一个节点，先补其全部 T 用例，再通过类型检查、完整 CLI 测试、构建和发布包 help 验证；未通过不得进入 dev。
5. **dev 契约探针**：先用最小隔离资源验证 B-01~B-06；探针成功才允许写相应业务功能的端到端资源。
6. **dev 首次发行闭环**：按 C-03 → C-06/C-07 → C-11/C-12 → C-14~C-18 → C-17/C-20 执行；每一步保存 resourceId、请求类型、读回字段与状态。
7. **dev 发行后管理闭环**：在同一已发布合集上执行 C-08/C-09/C-11/C-12/C-14~C-21 的正反分支，再二次 publish，确认已发布目录/快照变化符合 intent。

## 7. dev 数据与清理约束

| 角色 | 最小数据要求 | 作用 |
|---|---|---|
| 集合测试账号 | `freelog*` dev 账号，凭据仅来自忽略的本地 credential 文件 | 创建/维护合集的 owner |
| 本人上架普通单资源 | 至少 2 个，包含可排序/移除/重命名单品 | C-06/C-08 |
| 本人上架且有上抛的单资源 | 至少 1 个 | C-07 的授权排除映射 |
| 可签免费上游策略 | 至少 1 个当前合集未签约样本 | 显式策略签约 |
| 可签付费上游策略 | 至少 1 个当前合集未签约样本 | 验证“签约继续、浏览器支付”边界 |
| 支持可选配置的合集叶子 | 至少 1 个 | C-15 |
| 不支持可选配置的合集叶子 | 至少 1 个 | C-15 负向门禁 |
| 自动收录合集 | 可维护的隔离样本；若无则 B-02 保持 BLOCKED | 自动/手工目录并发负向测试 |

测试资源原则上可保留为未上架审计壳；已经上架的单资源或合集不得为“清理测试”删除用户既有内容。每次 dev 运行以独立前缀命名，并在测试报告记录保留/下架/删除决策。

### 7.1 2026-09-18 dev 本轮证据摘要

两个隔离的未上架合集壳保留为审计样本（真实 ID 只留在本地临时工程，不写入仓库）。所有请求均使用 `freelog*` dev 账号与 CLI 的 tools-lib Node 入口；没有写入 prod、没有输出凭据。

| 覆盖 | dev 读回结论 | 未覆盖边界 |
|---|---|---|
| C-03/C-04 | 直接叶子创建成功；另一个临时工程首次 bind 与重复 bind 均幂等，身份是 `subject:"collection"` 且无 `filePath` | 类型层级/搜索交互、错误 owner/环境分支 |
| C-05/C-06/C-08/C-09 | 三个既有单品的授权状态读取；新增本人已上架单资源后读回存在、移除后读回不存在；rename、move 与标题降序重排均读回 | >100 分页、搜索、上游授权样本 |
| C-11/C-12/C-13 | 标题、简介、标签、PNG 封面组合更新由命令内部远端重读确认；规则空响应、开启、关闭均确认；未发布目录拒绝开启自动，开启后目录写拒绝，关闭后恢复写入 | listing 边界值、自动规则实际命中、跨页目录比较 |
| C-14/C-15/C-16/C-18 | 表单拉取/编辑/发布/再拉取；属性、文本配置和下拉配置候选值完整读回；免费直接依赖签约并读回；无本地表单稿的目录变更也可直接 merge 发布 | 已有非空 `authExcludedItems`、未知 POST |
| C-23/C-24 | 更新日志读取真实 CREATE/ADD 记录；合集作为授权方的合约列表空结果可读 | 多页、各合同状态 |

## 8. 通过判定与报告格式

### 8.1 节点状态

每个 C-ID 只能取以下状态之一：

- `NOT_STARTED`：没有代码或测试；
- `DESIGN_READY`：E1/E2（需要时 E3）齐全，尚未实现；
- `LOCAL_PASS`：E4 通过，尚未 dev；
- `DEV_PASS`：E1~E5 齐全；
- `BLOCKED`：缺少 B-ID 对应外部契约；
- `DECISION_REQUIRED`：产品边界尚未定案，不能用实现猜测代替决定；
- `OUT_OF_SCOPE`：一期明确不提供，但已有目标必须有保护性负向测试；
- `FAIL`：存在可重现的设计、实现或平台偏差。

“当前合集全流程通过”唯一条件：C-01～C-24 中所有已纳入一期的节点均为 `DEV_PASS`，或用户明确从一期范围删除某一节点并同步本表、正式设计和交接文档；C-25 维持 `OUT_OF_SCOPE` 并完成 T-52 的保护性测试。RSS 重新排期时，必须恢复其 C/T/B 节点并另行完成 dev 证据，不能沿用当前“合集全流程通过”的表述。任何 `BLOCKED`、`DECISION_REQUIRED`、`NOT_STARTED`、`LOCAL_PASS` 或 `FAIL` 都不能写为当前范围的全流程通过。

### 8.2 每次测试报告最小字段

```json
{
  "caseId": "T-xx",
  "nodeIds": ["C-xx"],
  "environment": "dev",
  "result": "PASS | BLOCKED | FAIL",
  "resourceIds": ["redacted-or-test-id"],
  "requestEvidence": "method + payload field names; no secrets",
  "readbackEvidence": "which stable fields were re-read",
  "cleanup": "kept | offline | removed | not-applicable",
  "blocker": "B-xx or null"
}
```

## 9. 可追溯来源

- 产品/流程：[合集总览](../README.md)、[首次发行总览](../创建/00-总览.md)、[单品管理](../单品/01-添加单品.md)、[自动收录](../单品/02-自动收录与手动目录.md)、[单品编辑](../单品/03-编辑移除排序与授权状态.md)、[合集信息](../信息/01-完善合集信息.md)、[合集表单](../表单/01-属性-可选配置与展示.md)、[直接依赖](../表单/02-直接依赖与签约.md)、[发布恢复](../发布/01-发布合集变更与恢复.md)、[策略上下架](../策略与上架/01-策略与上下架.md)、[只读日志与授权合约](../维护/01-只读日志与授权合约.md)、[RSS 来源接入](../RSS/01-RSS来源接入与同步边界.md)。
- 可信字段规则：[字段级校验对照表](../../../../业务梳理/字段级校验对照表.md)。
- Node API 事实：`packages/tools-lib/src/service-API/resources.ts` 的 `updateCollection`、目录 published/draft 接口、收录规则、授权状态与授权更新接口；`packages/tools-lib/src/service-API/contracts.ts` 的合同查询/签约接口。
- 已实现代码与局部测试：`packages/cli/src/domain/collection/`、`packages/cli/src/commands/collection/`、`packages/cli/tests/domain/collection-create.test.ts`。
- Console 可执行行为：`D:/appinside/freelogfe-web-repos/packages/console/src/models/collectionManager/`、`pages/resource/collectionSidebar/`、`components/FAddResourcesHandleAuth/`。注释、archive 和旧 CLI 不构成证据。

## 10. 本文维护规则

1. 新增/删改任何合集命令、接口或状态文件时，必须先更新 C-ID 图和对应 T-ID；否则不允许进入代码评审。
2. 新发现接口响应字段、Console 映射或平台差异时，先记录为 B-ID 或更新现有证据等级；不得直接用 `any`、默认值或“先写再看”掩盖。
3. 本文通过复核后，实施顺序严格按第 6 节；每完成一个节点才更新其状态，不提前把设计写为已验证。
