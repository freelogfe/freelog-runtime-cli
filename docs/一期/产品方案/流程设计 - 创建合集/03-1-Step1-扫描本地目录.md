# F3.1 · Step1 - 扫描本地目录详细流程

> **文档角色**: 覆盖 Step1"扫描本地目录"的全部细节  
> **对齐 Source**: CLI 的 CollectionScanner 实现  
> 最后更新：2026-09-02

---

## 📋 **一、Step1 流程总览**

```bash
$ freelog collection-create ./my-podcasts/

┌─ Freelog 合集创建 ───────────────────────────────┐
│                                                     │
│ Step 1: 扫描本地目录                                  │
│                                                     │
│ ▼ 扫描配置                                          │
│   • 入口目录：./my-podcasts/                        │
│   • 匹配规则：episode.json, feed.xml                │
│   • 跳过隐藏文件：.git/, .DS_Store                   │
│   • GUID 验证：必需                                  │
│                                                     │
│ ▓▓▓▓▓▓▓▓▓░░ 80%                                     │
│ 当前：Episode-012-Guiding-Principles.json            │
│                                                     │
│ ✅ 扫描完成                            12 个条目     │
│    有效：12 | 无效：3 | 总扫描文件：120                │
│    类型识别：Podcast                                │
│                                                     │
│ ▶ [详情] 查看条目列表                                 │
│    ✓ Episode-001-AI-Coding                          │
│    ✓ Episode-002-Memory-System                      │
│    ✗ Broken-Episode (缺少 guid)                     │
│                                                     │
│ 下一步：ENTER | 取消：ESC                            │
└─────────────────────────────────────────────────────┘
```

---

## 🔍 **二、详细步骤实现**

### **2.1 扫描规则定义**

```typescript
interface CollectionScanRules {
  // 入口目录
  rootDir: string;
  
  // 匹配规则
  episodePatterns: string[];  // ['episode.json', 'feed.xml']
  feedPatterns: string[];     // ['feed.xml', 'rss.xml']
  ignorePatterns: string[];   // ['.git/*', '.DS_Store', 'node_modules/**']
  
  // 过滤选项
  skipHiddenFiles: true;
  skipInvalidGuids: false;    // 是否跳过无 GUID 的条目
}
```

### **2.2 CollectionScanner 核心逻辑**

```typescript
class CollectionScanner {
  async scan(config: ScanConfig): Promise<CollectionScanResult> {
    const episodes: EpisodeEntry[] = [];
    const invalidEpisodes: InvalidEntry[] = [];
    
    // 1. 递归扫描所有可能的 episode 文件
    const allFiles = await this.readdirRecursive(config.rootDir);
    
    // 2. 识别 episode 文件和 feed 文件
    const episodeFiles = allFiles.filter(f => 
      config.episodePatterns.some(p => f.endsWith(p))
    );
    
    const feedFile = allFiles.find(f => 
      config.feedPatterns.some(p => f.endsWith(p))
    );
    
    // 3. 解析每个 episode
    for (const filePath of episodeFiles) {
      try {
        const episode = await this.parseEpisode(filePath);
        
        // GUID 是唯一必需字段
        if (!episode.guid || episode.guid.length === 0) {
          invalidEpisodes.push({
            path: filePath,
            reason: 'MISSING_GUID',
            errorMessage: 'episode 缺少必填字段：guid'
          });
          continue;
        }
        
        episodes.push(episode);
      } catch (err) {
        invalidEpisodes.push({
          path: filePath,
          reason: 'PARSE_ERROR',
          errorMessage: err.message
        });
      }
    }
    
    // 4. 检测是否为播客类型
    const contentType = this.detectContentType(episodes, feedFile);
    
    return {
      validEpisodes: episodes,
      invalidEpisodes,
      hasFeedFile: !!feedFile,
      contentType,  // 'podcast' / 'video-series' / 'audio-series' / 'unknown'
      statistics: this.computeStatistics(episodes, invalidEpisodes)
    };
  }
  
  private detectContentType(
    episodes: EpisodeEntry[],
    feedFile: string | undefined
  ): CollectionType {
    if (!feedFile && episodes.every(e => !e.mediaType)) {
      return 'static-collection';
    }
    
    if (feedFile && feedFile.includes('rss')) {
      return 'podcast';
    }
    
    return 'dynamic-collection';
  }
}
```

### **2.3 Episode 解析逻辑**

```typescript
async function parseEpisode(filePath: string): Promise<EpisodeEntry> {
  const content = fs.readFileSync(filePath, 'utf-8');
  const ext = path.extname(filePath);
  
  let episodeData: EpisodeSchema;
  
  switch (ext) {
    case '.json':
      episodeData = JSON.parse(content);
      break;
      
    case '.xml':
    case '.rss':
      episodeData = parseRssItem(content);
      break;
      
    default:
      throw new ValidationError('UNSUPPORTED_FORMAT', '不支持的 episode 格式');
  }
  
  // 验证必填字段
  const validated = EpisodeSchema.parse(episodeData);
  
  return {
    path: filePath,
    directory: path.dirname(filePath),
    guid: validated.guid!,
    title: validated.title,
    description: validated.description,
    mediaType: validated.mediaType || 'audio',
    pubDate: validated.pubDate ? new Date(validated.pubDate) : null,
    duration: validated.duration || null,
    author: validated.author || null,
    isValid: true
  };
}

// RSS 解析器
function parseRssItem(rssContent: string): EpisodeSchema {
  const parser = new DOMParser();
  const xmlDoc = parser.parseFromString(rssContent, "text/xml");
  
  const item = xmlDoc.querySelector('item');
  if (!item) {
    throw new Error('RSS 中未找到 episode 条目');
  }
  
  return {
    guid: item.querySelector('guid')?.textContent || '',
    title: item.querySelector('title')?.textContent || '',
    description: item.querySelector('description')?.textContent || '',
    pubDate: item.querySelector('pubDate')?.textContent || '',
    enclosure: {
      url: item.querySelector('enclosure@url')?.textContent || '',
      type: item.querySelector('enclosure@type')?.textContent || '',
      length: item.querySelector('enclosure@length')?.textContent || ''
    }
  };
}
```

### **2.4 TTY 进度渲染器**

```typescript
class ScanProgressRenderer {
  render(scanned: number, total: number, currentFile: string) {
    ui.clearLine();
    
    const percent = Math.round((scanned / total) * 100);
    
    console.log(`\r${ui.color.info}[${percent}%]${ui.color.reset} `);
    console.log(`${'▓'.repeat(Math.floor(percent / 10))}${
      '░'.repeat(10 - Math.floor(percent / 10))} ${scanned}/${total}`);
    
    if (currentFile) {
      console.log(ui.dim(`当前：${path.basename(currentFile)}`));
    }
  }
  
  showSummary(result: CollectionScanResult) {
    console.log('\n\n┌─ 扫描结果 ─────────────────────┐');
    console.log(`│ 有效条目：${result.validEpisodes.length}                    │`);
    console.log(`│ 无效条目：${result.invalidEpisodes.length}                   │`);
    console.log(`│ 总扫描文件：${result.statistics.totalFiles}                  │`);
    console.log(`│ 类型识别：${this.formatContentType(result.contentType)}      │`);
    console.log('└─────────────────────────────────┘\n');
    
    if (result.invalidEpisodes.length > 0) {
      console.log('⚠️  无效条目列表:');
      result.invalidEpisodes.slice(0, 5).forEach(entry => {
        console.log(`  ✗ ${entry.path}`);
        console.log(`    原因：${this.formatReason(entry.reason)}`);
      });
    }
    
    console.log('\n下一步：ENTER | 查看详细：D | 取消：ESC\n');
  }
  
  private formatContentType(type: CollectionType): string {
    const typeNames: Record<string, string> = {
      'podcast': '播客 Podcast',
      'video-series': '视频系列 Video Series',
      'audio-series': '音频系列 Audio Series',
      'static-collection': '静态合集 Static Collection'
    };
    return typeNames[type] || type;
  }
  
  private formatReason(reason: string): string {
    const reasons: Record<string, string> = {
      'MISSING_GUID': '缺少必填字段：guid',
      'PARSE_ERROR': 'JSON/YAML 解析失败',
      'DUPLICATE_GUID': 'GUID 重复',
      'UNSUPPORTED_FORMAT': '不支持的文件格式'
    };
    return reasons[reason] || reason;
  }
}
```

---

## 🚨 **三、异常分支处理**

| 异常场景 | 错误码 | 用户提示 | 处理方式 |
|---------|--------|---------|---------|
| 文件不是有效 JSON | MANIFEST_PARSE_ERROR | "无法解析 episode.json" | 加入 invalidEpisodes |
| 缺少必填字段 | MISSING_GUID | "缺少字段：guid" | 加入 invalidEpisodes |
| GUID 重复 | DUPLICATE_GUID | "发现重复 GUID" | 终止并提示修改 |
| 磁盘 I/O 错误 | DISK_IO_ERROR | "读取文件失败" | 尝试 3 次后标记为 invalid |
| 权限不足 | PERMISSION_DENIED | "无法访问：{path}" | 记录警告但继续扫描 |

---

## ✅ **四、验收标准**

| 测试项 | 预期行为 | 验证方法 |
|-------|---------|---------|
| 递归扫描 | 正确遍历所有子目录 | Mock 多层级目录 |
| Episode 识别 | 仅匹配 episode.json/feed.xml | 测试多种文件名 |
| GUID 验证 | 缺失时标记为无效 | 构造无 GUID 的 episode |
| RSS 解析 | 正确解析 XML 格式 | 提供真实 RSS 内容 |
| 类型识别 | 自动识别 Podcast/VideoSeries | 不同组合测试 |
| 进度展示 | 实时百分比 + 当前文件 | 观察 UI 更新 |
| Checkpoint 恢复 | 中断后可继续 | Ctrl+C 后 restart 验证 |

---

## 🔗 **五、相关文档索引**

| 文档 | 说明 | 路径 |
|-----|------|------|
| [F3 总纲](../../流程设计/03-创建合集总纲.md) | 完整的 F3 流程导航 | 流程设计 |
| [Step2](./03-2-Step2-填写合集信息.md) | 下一步骤 | 流程设计 |

---

**下一步**: 阅读 **[03-2-Step2-填写合集信息](./03-2-Step2-填写合集信息.md)**
