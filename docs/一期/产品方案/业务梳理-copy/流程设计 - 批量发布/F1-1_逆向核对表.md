# 🎯 F1-1 批量发布 - 数据提交角度逆向核对表

> **核心原则**: 从 createBatch API 的数据结构出发，反向追踪前端组件中的 UI 约束  
> **数据来源**: Console Handle/index.tsx L807-947 (createResourceObjects 构造)

---

## ✅ 一、createResourceObjects 完整字段列表及 UI 追溯

### **必填基础字段** (所有文件都有对应输入)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| resourceName | `name` | resourceName_optimized | Step2 L362-364 | ≤50 字符 | Handle L808 | ✅ 必须记录 |
| resourceTitle | `resourceTitle` | resourceTitle | Step2 L366 | ≤100 字符 | Handle L809 | ✅ 必须记录 |
| version | `version` | 固定值 | 无 UI | 固定'1.0.0' | Handle L819 | ❌ CLI 自动处理 |
| fileSha1 | `fileSha1` | sha1 | Step2 L176-181 | 40 字符 SHA1 | Handle L820 | ✅ 后端计算 |
| filename | `filename` | fileName | Step2 L176-181 | 原始文件名 | Handle L821 | ✅ 自动传递 |

**UI 约束总结**:
```typescript
// Step2 资源名生成 (L362-366)
const resourceName: string = getARightName(
  FRegExpMgr.removeExtension(f.name)
).substring(0, 50);  // ← UI 限制 50 字符!

const resourceTitle: string = f.name.replace(
  new RegExp(/\.[\w-]+$/), ''
).substring(0, 100);  // ← UI 限制 100 字符!
```

---

### **策略相关字段** (F0-2 Step3 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| policyName | `policies[].policyName` | policy.title | Step3 FPolicyList | 非空字符串 | Handle L811 | ✅ 必填 |
| policyText | `policies[].policyText` | policy.text | Step3 FPolicyList | 完整策略文本 | Handle L812 | ✅ 必填 |
| status | `policies[].status` | 固定 1 | 无 UI | 固定 1(启用) | Handle L813 | ❌ 固定值 |

**UI 组件**:
```typescript
// Step3 FPolicyList 展示策略
{step3_policies.length > 0 && (
  <FPolicyList
    dataSource={step3_policies}  // [{id, title, text, isUsing}]
  />
)}
```

**CLI 特殊说明**:
- ❌ Console 允许无策略提交 (L193 disabled=false)
- ✅ CLI 应强制至少一个免费策略

---

### **封面图片字段** (F0-4 Step4 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| coverImages | `coverImages[]` | step2_videoCover/封面上传 | FUploadCover | ≤5MB, JPG/PNG/GIF | Handle L815 | ✅ 尺寸格式 |

**UI 组件**:
```typescript
// Step4 封面上传 (L66-103)
<FUploadCover
  onUploadSuccess={(url) => {
    dispatch(onChange_step4_resourceCover({ value: url }));
  }}
>
  {/* JPG/PNG/GIF, ≤5M */}
</FUploadCover>
```

**CLI 实现**:
- 可自动生成封面或用户上传
- 需校验图片和视频大小

---

### **标签字段** (F0-4 Step4 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| tags | `tags` | resourceLabels | fEditLabelsDrawer | 最多 10 个 | Handle L817 | ✅ 数量限制 |

**UI 组件**:
```typescript
// Step4 标签编辑 (L197, L226)
await fEditLabelsDrawer({
  labels: [],
  resourceTypeCode: ...
});
```

**注意**: 
- fEditLabelsDrawer 内部可能限制 maxTags=10
- 需要查看该组件源码确认

---

### **描述字段** (可选，实际未使用)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| intro | `intro` | 固定'' | 无 UI | 固定空字符串 | Handle L816 | ❌ 跳过 |
| description | `description` | 固定'' | 无 UI | 固定空字符串 | Handle L822 | ❌ 跳过 |

**结论**: 
- Console 中这两个字段都填了空字符串
- **没有对应的 UI 输入组件**
- **CLI 不需要处理这些字段!**

---

### **依赖关系字段** (Step3 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| dependencies | `dependencies[]` | directDependencies | FMicroAPP_Authorization | 微前端授权 | Handle L823 | ❌ CLI 不支持 |
| dependency.versionRange | `dependencies[].versionRange` | 版本范围 | 下拉选择 | 版本号格式 | Handle L825 | ❌ 不处理 |

**UI 组件**:
```typescript
// Step2 微前端授权 (L902-924)
<FMicroAPP_Authorization
  depList={step2_directDependencies}
  update={(data) => {
    setDirectDependencies(data.directDependencies);
  }}
/>
```

**结论**:
- FMicroAPP_Authorization 是微前端组件
- **CLI 完全不需要依赖这个功能!**

---

### **自定义属性字段** (Step2 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| customPropertyDescriptors | `customPropertyDescriptors[]` | systemProperties + customProperties | FAttrsAndConfigs | 最多 30 个 | Handle L827 | ✅ 必填 |
| - key | `customPropertyDescriptors[].key` | 属性键 | 文本输入 | 唯一性 | Handle L828 | ✅ 不可重复 |
| - name | `customPropertyDescriptors[].name` | 属性名称 | 文本输入 | 唯一性 | Handle L829 | ✅ 不可重复 |
| - defaultValue | `customPropertyDescriptors[].defaultValue` | 属性值 | 文本输入 | maxLength=100 | Handle L830 | ✅ 长度限制 |
| - type | `customPropertyDescriptors[].type` | 只读文本 | 固定值 | 'readonlyText' | Handle L831 | ❌ 固定 |
| - candidateItems | `customPropertyDescriptors[].candidateItems` | select 选项 | 数组 | 可选配置 | Handle L832 | ✅ 可选 |

**UI 组件**:
```typescript
// Step2 属性编辑 (L579-672)
<FAttrsAndConfigs
  dateSource={[
    ...systemProperties.map(sp => ({
      key: sp.key,
      value: sp.value,
      configs: {
        value_Editable: sp.type === 'additional' && sp.valueConfig,
      },
    })),
    ...customProperties.map(cp => ({
      key: cp.key,
      value: cp.value,
      configs: {
        value_Editable: {
          text: { nullable: true, minLength: 0, maxLength: 100 }  // ← L611!
        },
      },
    })),
  ]}
/>
```

**关键约束**:
- ✅ **最多 30 个自定义属性** (L484)
- ✅ **自定义属性值 maxLength=100** (L611)
- ✅ **key/name 不可重复** (disabledKeys/disabledNames)

---

### **向上兼容资源字段** (Step2 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| baseUpcastResources | `baseUpcastResources[]` | baseUpcastResources | FMicroAPP_Authorization | 微前端授权 | Handle L835 | ❌ CLI 不支持 |

**结论**: 同 dependencies，属于微前端授权

---

### **系统输入属性字段** (Step2 解析)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| inputAttrs | `inputAttrs[]` | systemProperties[additional] | 属性解析 | 类型匹配 | Handle L838 | ✅ 必输 |
| - key | `inputAttrs[].key` | 属性键 | 系统解析 | 固定值 | Handle L839 | ❌ 自动 |
| - value | `inputAttrs[].value` | 属性值 | 用户编辑 | 根据 valueConfig | Handle L840 | ✅ 根据类型 |

**UI 组件**:
```typescript
// Step2 系统属性展示 (L549-674)
{...systemProperties.filter(sp => sp.type === 'additional').map(ap => ({
  key: ap.key,
  value: ap.value,
}))}
```

**说明**:
- type='raw'的系统属性不传 inputAttrs
- 仅 type='additional'的系统属性会传输

---

### **签约合约字段** (Step2 管理)

| 字段 | JSON 名称 | UI 输入位置 | 字段来源 | UI 约束 | Console 证据 | CLI 关注 |
|------|----------|-----------|---------|--------|-------------|---------|
| batchSignContracts | `batchSignContracts[]` | batchSignContracts | Step2/F0-3 | 至少一个 | Handle L842 | ✅ 必填 |
| - resourceId | `batchSignContracts[].resourceId` | resourceID | 资源 ID | 格式校验 | Handle L843 | ✅ 必填 |
| - policyIds | `batchSignContracts[].policyIds` | policyIDs | 策略 ID 数组 | 非空 | Handle L844 | ✅ 必填 |
| - subjectType | `batchSignContracts[].subjectType` | subjectType | 主体类型 | 固定值 | Handle L845 | ❌ 固定 |

**说明**:
- 与 policies 字段类似，但用于签约操作
- CLI 需要映射为 contract 创建逻辑

---

## 📊 二、总结：CLI 真正需要关注的字段

### **✅ 必须实现的字段 **(有 UI 输入)

| 优先级 | 字段组 | 数量 | 说明 |
|--------|--------|------|------|
| P0 | name/resourceTitle | 2 | 资源标识 + 标题 |
| P0 | policies | ≥1 | 授权策略 (免费) |
| P0 | customPropertyDescriptors | 0-30 | 自定义属性 |
| P1 | coverImages | 0-1 | 封面图片 |
| P1 | tags | 0-10 | 标签列表 |
| P1 | inputAttrs | 0-N | 系统输入属性 |
| P1 | batchSignContracts | ≥1 | 签约信息 |

### **❌ 不需要处理的字段 **(无 UI 输入)

| 字段 | 原因 |
|------|------|
| version | 固定'1.0.0' |
| fileSha1/filename | 后端自动计算 |
| intro/description | Console 固定空字符串 |
| dependencies/baseUpcastResources | 微前端功能 |
| status | 固定 1(启用) |

---

## 🔍 三、关键发现

### **1. 字段覆盖不全**
- Console 创建了 `customPropertyDescriptors`,但实际上是从`systemProperties/customProperties/customConfigurations` 三个地方聚合而来
- **CLI 需要理解这个转换逻辑** (Handle L881-914)

### **2. 某些字段被忽略**
- `intro` 和 `description` 都是空字符串
- **Console 也没用这些字段!**
- **CLI 完全跳过即可**

### **3. 微前端依赖排除**
- `dependencies` 和`baseUpcastResources`来自 FMicroAPP_Authorization
- **CLI 不支持此功能，直接忽略**

### **4. 真正的 UI 输入点**
```typescript
// 实际有 UI 输入的字段:
- name               → Step2 L362 (自动生成)
- resourceTitle       → Step2 L366 (自动生成)
- policies           → Step3 FPolicyList
- coverImages         → Step4 FUploadCover
- tags                → Step4 fEditLabelsDrawer
- customProperties   → Step2 FAttrsAndConfigs (L579)
- customConfigurations → Step2 FResourceOptions (L825)
- systemProperties   → Step2 FAttrsAndConfigs (L579)
```

---

## ✅ 四、修正建议

### **原 F1-1 文档需要修正的地方**

#### 1. **删除错误字段**
- ❌ intro/description (无 UI 输入)
- ❌ dependencies/baseUpcastResources (微前端)

#### 2. **补充完整逻辑**
- ✅ customPropertyDescriptors 的转换流程 (L881-914)
- ✅ inputAttrs 仅包含 type='additional'的系统属性

#### 3. **精确到 UI 组件**
```markdown
## 字段约束对照表 (基于真实 UI 输入)

| 字段 | UI 组件 | 约束 | Console 证据 |
|------|--------|------|-------------|
| name | 自动生成算法 | ≤50 | Handle L362-364 |
| resourceTitle | 自动生成算法 | ≤100 | Handle L366 |
| policies | FPolicyList | ≥1 | Step3 L121 |
| coverImages | FUploadCover | ≤5MB | Step4 L66 |
| tags | fEditLabelsDrawer | ≤10 | Step4 L197 |
| customProperties | FAttrsAndConfigs | 0-30, value≤100 | Step2 L484,L611 |
| customConfigurations | FResourceOptions | 0-30 | Step2 L736 |
| systemProperties | FAttrsAndConfigs | 系统解析 | Step2 L549 |
```

---

**End of Reverse Verification Report**
