# F1.2 · Step2 - 上传资源与属性配置详细流程

> **文档角色**:覆盖 Console Step2"上传资源与属性配置"的完整源码对齐  
> **对齐 Source**:Console `packages/console/src/pages/resource/creator/Step2/index.tsx` (L1-1016)  
> 最后更新:2026-09-02

---

## 📋 **一、流程总览**

```
┌─ Freelog 资源发行 ───────────────────────────────┐
│                                                   │
│ Step 2/4: 上传资源与属性配置                        │
│                                                   │
│ ▼ 本地文件上传                                    │
│   拖拽或点击选择 artifact.zip                     │
│                                                   │
│ ▓▓▓▓▓▓▓▓▓▓ 100%                                  │
│ ✅ 文件上传成功           FileID=yyy              │
│    大小:5.2 MB | SHA1:a1b2c3d...                  │
│                                                   │
│ ▶ 自动解析基础属性                                │
│   ✓ mime: application/zip                         │
│   ✓ size: 5,456,789 bytes                           │
│   ✓ version: 1.0.0                                │
│   ✓ author: liu-kai-github                        │
│                                                   │
│ [+ 补充属性] [更多设置 ↓]                          │
│                                                   │
│ ⚠️ 依赖授权管理 (暂不支持支付签约)                    │
│                                                   │
│ [下一步 →]                                        │
└───────────────────────────────────────────────────┘
```

**核心环节**:
1. 本地文件上传 (仅实现此方式)
2. 自动解析系统属性
3. 补充属性手动添加
4. 可选配置 (input/select 类型)
5. 依赖授权 (免费策略签约)

---

## 🚀 **二、Console 源码证据**

### **2.1 文件上传组件 (L168-225)**

```typescript
// packages/console/src/pages/resource/creator/Step2/index.tsx
{resourceCreatorPage.step2_resourceTypeConfig.uploadEntry.includes('localUpload') && (
  <FLocalUpload
    limitFileSize={resourceCreatorPage.step2_resourceTypeConfig.limitFileSize}
    resourceTypeCode={resourceCreatorPage.step1_createdResourceInfo?.resourceTypeCode || ''}
    onSucceed={(value) => {
      dispatch({ type: 'onSucceed_step2_localUpload', payload: { fileName: value.fileName, sha1: value.sha1 } });
    }}
  />
)}

// 其他上传方式 (CLI 暂不实现)
{resourceCreatorPage.step2_resourceTypeConfig.uploadEntry.includes('storageSpace') && <FStorageSpace />} // 云端存储
{resourceCreatorPage.step2_resourceTypeConfig.uploadEntry.includes('markdownEditor') && <FResourceMarkdownEditor />} // Markdown 编辑
{resourceCreatorPage.step2_resourceTypeConfig.uploadEntry.includes('cartoonEditor') && <FResourceCartoonEditor />} // 卡通动画
```

**【CLI 说明】**:仅实现 `localUpload`,其他 3 种方式暂不支持

---

### **2.2 文件操作按钮 (L336-428)**

```typescript
// 编辑按钮
{isSupportEdit && <FTextBtn onClick={() => dispatch({ type: 'onClick_step2_editMarkdownBtn' })}>✏️ 编辑</FTextBtn>}

// 下载按钮
{isSupportDownload && <FTextBtn onClick={() => location.href = `/v2/storages/files/${sha1}/download`}>📥 下载</FTextBtn>}

// 删除按钮
<FTextBtn type='danger' onClick={async () => {
  const confirm = await fPromiseModalConfirm({ description: '确定要移除此项吗？' });
  if (confirm) dispatch({ type: 'onRemove_step2_file' });
}}>🗑 删除</FTextBtn>
```

**【CLI 说明】**:暂不实现编辑/下载按钮，仅保留删除功能

---

### **2.3 视频封面上传 (L432-452)**

```typescript
if (isVideoResource) {
  <div className={styles.videoCoverRow}>
    <FUploadCover use={'resource'} onUploadSuccess={(url) => {
      dispatch({ payload: { step2_videoCover: url } });
    }}>
      <span>{videoCover ? '更换视频封面' : '上传视频封面'}</span>
    </FUploadCover>
  </div>
}
```

**【CLI 说明】**:暂不支持视频封面上传

---

### **2.4 基础属性显示 (L468-682)**

```typescript
// 系统属性 + 自定义属性
<FAttrsAndConfigs
  dateSource={[
    ...step2_systemProperties.map(sp => ({
      key: sp.key,
      value: sp.value,
      name: sp.name,
      description: sp.description,
      configs: { item_Deletable: false, key_Editable: false },
    })),
    ...step2_customProperties.map(cp => ({
      key: cp.key,
      value: cp.value,
      name: cp.name,
      description: cp.description,
      configs: { 
        item_Deletable: true,
        key_Editable: true,
        value_Editable: { text: { nullable: true, maxLength: 100 } },
      },
    })),
  ]}
  onChangeItem={(oldData, newData, index) => { /* 更新状态 */ }}
  onDeleteItem={(value) => { /* 删除自定义属性 */ }}
/>

// 补充属性弹窗 (L484-545)
const dataSource = await fResourcePropertyEditor3({
  disabledKeys: [...step2_systemProperties, ...step2_customProperties, ...step2_customConfigurations],
  disabledNames: [...],
});
if (dataSource) {
  dispatch({ 
    type: 'onChange_step2_customProperties', 
    payload: { value: [...step2_customProperties, { ...dataSource, value: '' }] } 
  });
}
```

**业务规则**:
- 最多 30 个补充属性
- key/name 不能重复
- Value 文本框限制 100 字符

---

### **2.5 可选配置系统 (L719-896)**

```typescript
{$showMore && (
  <div>
    {isSupportOptionalConfig && (
      <div className={styles.block}>
        <div style={{ display: 'flex', justifyContent: 'space-between' }}>
          <h3>可选配置</h3>
          {step2_customConfigurations.length < 30 && (
            <FTextBtn onClick={async () => {
              const dataSource = await fResourceOptionEditor({
                disabledKeys: [...],
                disabledNames: [...],
              });
              if (dataSource) {
                dispatch({ 
                  type: 'onChange_step2_customConfigurations', 
                  payload: { value: [...step2_customConfigurations, dataSource] } 
                });
              }
            }}>
              [+ 添加配置]
            </FTextBtn>
          )}
        </div>

        {customConfigurations.length > 0 && (
          <FResourceOptions
            theme={'dark'}
            dataSource={customConfigurations}
            onEdit={async (value) => { /* 编辑逻辑 */ }}
            onDelete={async (value) => { /* 删除逻辑 */ }}
          />
        )}
      </div>
    )}
  </div>
)}
```

**可选配置结构**:
```typescript
interface OptionConfig {
  key: string;         // 唯一标识
  name: string;        // 显示名称
  type: 'input' | 'select';
  input?: string;      // Input 类型的默认值
  select?: string[];   // Select 类型的选项数组 (分号分隔)
  description?: string;// 描述说明
}
```

**业务规则**:
- 最多 30 个可选配置
- 需勾选"支持可选配置"才显示
- key 需全局唯一

---

### **2.6 依赖授权管理 (L902-924)**

```typescript
<FMicroAPP_Authorization
  licenseeId={resourceId}
  mainAppType={'resourceInVersionUpdate'}
  depList={directDependencies}      // [{id, name, type, versionRange}]
  upcastList={baseUpcastResources}  // [{resourceID, resourceName}]
  update={(data) => {
    dispatch({
      payload: {
        step2_directDependencies: data.directDependencies,
        step2_baseUpcastResources: data.baseUpcastResources,
        step2_isCompleteAuthorization: data.isAllAuthComplete ?? true,
      },
    });
  }}
/>
```

**【CLI 实现】**:仅支持免费策略签约
```bash
# Step1:生成模板
freelog-cli dep init-auth-map --out auth-map.yaml

# Step2:编辑后执行
freelog-cli dep auth --policy-map auth-map.yaml

# 检测到付费策略时拒绝并提示 ConsoleURL
```

---

### **2.7 Draft 自动保存机制 (L66-78)**

```typescript
AHooks.useDebounceEffect(
  () => {
    if (step2_dataIsDirty_count !== 0) {
      dispatch({ type: 'onTrigger_step2_SaveDraft' });
    }
  },
  [step2_dataIsDirty_count],
  { wait: 300 }, // 数据变化后 300ms 自动保存
);
```

**【CLI 实现】**:每个 Step 完成时自动保存 checkpoint JSON

---

### **2.8 Markdown/Cartoon Drawer (L981-1007)**

```typescript
<FMicroApp_MarkdownEditorDrawer
  open={step2_isOpenMarkdown}
  resourceID={resourceId}
  onChange_Saved={(saved) => {}}
  onClose={async () => { /* 关闭逻辑 */ }}
/>

<FMicroApp_CartoonEditorDrawer
  open={step2_isOpenCartoon}
  resourceID={resourceId}
  onClose={() => { /* 关闭逻辑 */ }}
/>
```

**【CLI 说明】**:暂不支持在线编辑器

---

### **2.9 提交按钮逻辑 (L961-976)**

```typescript
<FRectBtn
  disabled={!step2_fileInfo || !step2_isCompleteAuthorization}
  type={'primary'}
  onClick={() => {
    dispatch({ type: 'onClick_step2_submitBtn' });
  }}
>
  下一步
</FRectBtn>
```

**约束条件**:
- 必须上传文件 (`step2_fileInfo`)
- 必须完成所有依赖授权 (`step2_isCompleteAuthorization`)

---

## 🎯 **三、Summary**

| 环节 | Console 实现 | CLI 实现 |
|-----|------------|---------|
| 本地上传 | ✅ FLocalUpload | ✅ 实现 |
| 存储空间 | ✅ FStorageSpace | ❌ 不支持 |
| Markdown 编辑器 | ✅ FResourceMarkdownEditor | ❌ 不支持 |
| 卡通编辑器 | ✅ FResourceCartoonEditor | ❌ 不支持 |
| 基础属性解析 | ✅ 自动解析 | ✅ 自动解析 |
| 补充属性弹窗 | ✅ fResourcePropertyEditor3 | ✅ 保留 |
| 可选配置弹窗 | ✅ fResourceOptionEditor | ✅ 保留 |
| 视频封面上传 | ✅ FUploadCover | ❌ 不支持 |
| 编辑/下载按钮 | ✅ FTextBtn | ❌ 不支持 |
| 依赖授权微前端 | ✅ FMicroAPP_Authorization | ⚠️ 简化版 |
| Draft 自动保存 | ✅ DebounceEffect | ✅ Checkpoint |

**注释**:所有 Console 源码证据已标注具体行号，可直接对照实现。CLI 不支持的功能已在对应章节备注。

---

**下一步**:阅读 [F1.3-配置授权策略](./01-3-Step3-配置授权策略.md)
