# 字段级校验对照表（Console 源码 → CLI）

最后更新：2026-09-07。**本文是字段规则的准绳清单**：每条规则给出 Console/微前端源码出处（文件:行号），并标注 CLI 是否一致。改 CLI 校验或改规格时先对这张表。

Console 仓：`D:\appinside\freelogfe-web-repos`（下称 `console@`）；依赖声明微前端仓：`D:\appinside\freelog-sub-modules\【1】dependencies-declarator`（下称 `dd@`）。行号为 2026-09-07 快照，漂移时按关键词重找。

---

## 1. 版本号

| 规则 | Console 出处 | CLI | 一致 |
|------|-------------|-----|------|
| 空 → 必填 | `console@components/FVersionInput/index.tsx:79-82` | 规格 01/02「不能为空」 | ✓ |
| `!semver.valid(v)` → 不合法 | `FVersionInput:83-86` | update-version gates | ✓ |
| 必须 `semver.gt(v, latest)` | `FVersionInput:87-89` | update-version gates（新号 > latest） | ✓ |

## 2. 自定义属性（readonlyText）

| 规则 | Console 出处 | CLI | 一致 |
|------|-------------|-----|------|
| 名称空 → 必填 | `console@components/fResourcePropertyEditor3/FResourcePropertyEditorDrawer/index.tsx:168-173` | ATTR_FIELDS | ✓ |
| 名称 ≤50（`alert_naming_convention_attribute_name`） | 同上:174-176 | ATTR_NAME_LONG | ✓ |
| 名称不重复（disabledNames 含且非自身 → 「名称不能重复」） | 同上:177-179 | ATTR_NAME_DUPLICATE（2026-09-07 补） | ✓ |
| 键空 → 必填 | 同上:199-201 | ATTR_FIELDS | ✓ |
| 键 ≤30 | 同上:202-203 | KEY_RE `{0,29}` 隐含（错误并入 FORM_KEY_INVALID） | ✓ |
| 键不重复（`alert_key_exist`） | 同上:204-206 | ATTR_DUPLICATE | ✓ |
| 键正则 `^[a-zA-Z]([a-zA-Z0-9_]{1,29})?$` | `console@packages/@freelog/tools-lib/src/utils/regexp.ts:12`（CUSTOM_KEY） | `parseLine.ts:19` KEY_RE 等价 | ✓ |
| 说明 ≤50（`alert_key_remark_length`） | 同上:252-255 | ATTR_REMARK_LONG | ✓ |
| **值 ≤140**（`value_Editable.text.maxLength`） | `console@pages/resource/versionCreator/$id/index.tsx:809-814`（=140）；`creator/Step2/index.tsx:607-613`（=100） | MAX_VALUE=140（2026-09-07 由 100 改 140，取版本创建页；Console 两入口自身不一致，记录在案） | ✓* |
| 值可空（`nullable: true`） | versionCreator:812 | attrAdd 值可空 | ✓ |
| 条数 <30 才显示「添加」 | `versionCreator/$id/index.tsx:676` | ATTR_FULL（≥30 拒） | ✓ |
| 键写下后 Console 可改 key | FAttrsAndConfigs `key_Editable: true` | CLI 不跟随（换键=删了重加），规格 01 已声明 | ✓（设计差异已记录） |

## 3. 可选配置（editableText / select）

| 规则 | Console 出处 | CLI | 一致 |
|------|-------------|-----|------|
| 仅 `resourceConfig.supportOptionalConfig === 2` 类型可加；回显时非 2 类型配置清空 | `console@models/resourceVersionCreatorPage.ts:639-641,526` | optionAdd supportOptionalConfig 检查（真实路径由 typeCode 映射） | ✓ |
| 名称 ≤50 / 不重复 | `console@components/fResourceOptionEditor/FResourceOptionEditorDrawer/index.tsx:249-258` | OPTION_NAME_LONG / OPTION_NAME_DUPLICATE（2026-09-07 补） | ✓ |
| 键 ≤30 / 不重复 / CUSTOM_KEY | 同上:311-320 | 同属性 | ✓ |
| 说明 ≤50 | 同上:363-366 | OPTION_REMARK_LONG | ✓ |
| 方式只有 文本(input)/下拉(select) | 同上:434-478 | OPTION_MODE | ✓ |
| 文本默认值 ≤140 | 同上:496-498 | OPTION_VALUE_LONG | ✓ |
| 下拉每条选项 ≤140 | 同上:611-614 | 同上 | ✓ |
| 选项条数 <30 | 同上:637 | MAX_OPTION_ITEM=30 | ✓ |
| 选项不重复（`alert_cutstom_option_value_exist`） | 同上:688-708（verifyDuplication） | OPTION_DUPLICATE_OPTION | ✓ |
| 下拉默认值 = 第一项（提交时 `defaultValue: options[0]`） | `console@models/resourceVersionCreatorPage.ts:794-806` | optionAdd `options[0]`；下拉不许写默认（OPTION_SELECT_DEFAULT） | ✓ |
| 配置条目 <30 | `versionCreator/$id/index.tsx:938` | OPTION_FULL | ✓ |
| 提交体：input→`editableText{defaultValue:input}`；select→`select{defaultValue:options[0],candidateItems}` | model:779-800 | buildVersionPayload 同构 | ✓ |

## 4. 依赖

| 规则 | Console/微前端 出处 | CLI | 一致 |
|------|--------------------|-----|------|
| 默认 versionRange = `^` + latestVersion | `dd@src/components/resource-item.vue:92-99` | depAdd `?? ^latest` | ✓ |
| 空 range 报错 | `dd@src/components/dep.vue:176-177` | CLI 永不空（默认兜底） | ✓ |
| `!semver.validRange` 报错 | `dep.vue:178-179` | DEP_RANGE | ✓ |
| `!semver.maxSatisfying(已发号, range)` 报「对不上」 | `dep.vue:180-181` | DEP_RANGE | ✓ |
| 环检测 `POST /v2/resources/{本资源id}/versions/cycleDependencyCheck` | `dd@src/api/request.ts:77-82` | depAdd/depRange DEP_CYCLE | ✓ |
| 提交体 dependencies `{resourceId, versionRange}` | `console@models/resourceVersionCreatorPage.ts:808-813` | buildVersionPayload 同构 | ✓ |
| 提交体 baseUpcastResources `{resourceId}` | model:802-804 | CLI 恒 `[]`（规格：不写上抛） | ✓（有意差异） |
| authExcludedItems：排除走 `excludedType:'contractId'` | model:721-732 | CLI 一期恒 `[]`（规格：菜单 5 不写排除项） | ✓（有意差异） |
| **发版前授权完成度门禁**：`isAllAuthComplete=false`（任一依赖/未排除上抛无启用合约）→ 拦「依赖中存在未获取授权的资源」 | `versionCreator/$id/index.tsx:141-145` + `model:713-717`；计算在 `dd@src/store/index.ts:250-288` | CLI 无此客户端门禁（签后写稿，授权生效由平台解析授权链判定；dev 真网已验证无合约也能过 createVersion） | △ 设计差异，规格已记录 |
| 授权状态查询 | `dd@src/api/request.ts:62-64`（`GET /v2/auths/resources/batchAuth/results`） | Resource.batchAuth | ✓ |
| 微前端在版本场景**不直接签约**（签约走授权处理器微前端/平台侧） | `dd@src/store/index.ts:321-322`（resourceInVersionUpdate 只回 isAllAuthComplete） | CLI 用 `POST /v2/contracts/batchSign` 直签（`subjects[].subjectType:1` 平台强校验） | △ 流程差异：CLI 用裸签约接口替代授权处理器，2026-09-07 裸 API 已真网验证 |

## 5. 资源创建（Step1）

| 规则 | Console 出处 | CLI | 一致 |
|------|-------------|-----|------|
| 标题空 → 必填；>100 拒 | `console@models/resourceCreatorPage/step1Effects.ts:51-54`；`creator/Step1/index.tsx:128`（lengthLimit=100） | CREATE_TITLE_REQUIRED / CREATE_TITLE_TOO_LONG | ✓ |
| 授权标识：`resourceNameOptimized` 规范化（非法字符→`_`），空 → 必填 | `step1Effects.ts:73,90-91` | normalizeResourceName（create.ts:28-33） | ✓ |
| 标识查重（`Resource.info` 查 `username/optimized` 已存在则拒） | `step1Effects.ts:93-103` | CREATE_NAME_TAKEN（create.ts:171-176） | ✓ |
| 资源类型必选 | `creator/Step1/index.tsx:295` | CREATE_TYPE_REQUIRED | ✓ |
| 提交体 `name/resourceTypeCode/resourceTitle(trim)` | `step1Effects.ts:124-129` | create.ts:264-268 | ✓ |

## 6. 上下架（版本发行成功页引导）

| 规则 | Console 出处 | CLI | 一致 |
|------|-------------|-----|------|
| 已上架（status===1）直接成功 | `console@pages/result/resource/version/create/success/$id/$version/index.tsx:166-168` | online 幂等（已上架再 online 平台幂等） | ✓ |
| 无任何策略 → 先加策略（addPolicies）再上架 | 同上:169-197 | online 门禁「至少一条启用策略」+ policy apply | ✓（拆分一致） |
| 全部停用 → 引导启用（updatePolicies `{policyId, status}`） | 同上:199-231 | policy set --on/--off | ✓（拆分一致） |
| 上架 = `Resource.update {status:1}` | 同上:233-237 | onlineResource | ✓ |

---

## 7. 已知的 Console 自身不一致（CLI 取舍记录）

1. **自定义属性值上限**：创建向导 Step2=100（`creator/Step2:611`），独立版本创建页=140（`versionCreator:813`）。CLI 取 140（版本表单等价物是 versionCreator）。若平台端实为 100，真网提交 140 长度值会被平台拒——实测见 §8。
2. Console 允许改已写下的 key（`key_Editable: true`），CLI 不跟随（规格 01 已声明）。
3. Console 提交前有客户端授权完成度门禁；平台 createVersion 本身不校验授权（dev 实测）。CLI 不做此门禁。

## 8. 真网实测记录

- 2026-09-07：dev 主链（login→create→create-version→policy→online→offline）通过；付费依赖直签（`subjects[].subjectType:1`）落 `authStatus=128` 通过。字段级新校验（值 140 / 名称查重）为纯客户端规则，随单测覆盖；待作者手工测试时用 Console 对照验证 140 值平台可接受。

## 9. 维护约定

改 Console 对照到的任何数字（30/50/100/140/60）或规则，必须：① 改这张表并带新行号；② 改 CLI 常量与文案；③ 补/改 `tests/domain/remaining.test.ts`「字段校验对齐 Console」用例；④ 同步 `脚手架设计/PHASE/单资源/版本表单/01-属性.md`、`02-可选配置.md`、`03-依赖.md`。
