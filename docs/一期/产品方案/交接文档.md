# 交接文档

最后更新：2026-09-07（第 2 次：依赖签约改为「不分免费/付费直签」；补充架构守卫与验证入口）

新会话第一入口。**不替代**产品真源。产品听 [脚手架设计](./脚手架设计/README.md)，代码放哪听 [开发](./开发/README.md)。

仓库：`D:\appinside\freelog-runtime-cli`  
只改本仓。不要改浏览器仓。回复用中文。

**终端先手**：每次新开 PowerShell / Shell，会先执行 `chcp 65001 | Out-Null; [Console]::OutputEncoding=[System.Text.Encoding]::UTF8`，再读写/跑命令；否则中文输出、路径、断言都可能乱码或失真。

---

## 0. 一屏

| | |
|--|--|
| 本期 | 普通单资源 CLI。合集 / F1 / `import-dir` / RSS / 支付 / 上抛 **不做** |
| 规格 | **已齐**。不要再设计命令、不要合并两条发行命令 |
| 技术选型 | **已定**：[02-技术选型](./开发/02-技术选型.md)。commander、inquirer、中文硬编码 + `// i18n: key` |
| 代码 | T0.1–T13 **已验证**。一期开发切片收口，无下一刀 |
| 旧 CLI | [archive/cli](../archive/cli/00-备份说明.md) **随时可看**，只供参考。`packages/cli` 同样只是参考，可清空或删除 |
| 联调 | `--env test` 或 `--env dev`。**prod 打平台一律失败**，正式发布前再撤 |
| 质量门 | 单测 90/90（含**注释守卫**：导出函数必须有头注释；**编码守卫**：禁 U+FEFF）+ typecheck + build。改动后全过才算完 |

主路径：

```
login
  创建     init? → create → create-version（无上一版，1.0.0）→ policy? → update? → online
  发新号   version draft pull? → 改缓存 → update-version
  接入     bind <id|username/name> [--file]
```

不用 `publish`。不用顶层 `release`。上架只用 `online`。`create-version` 和 `update-version` 不是同一条，禁止自动改口。

---

## 1. 新会话怎么接着干

建议开场（复制即可）：

```
继续一期 CLI。先读 docs/一期/产品方案/交接文档.md。
产品真源是脚手架设计 README「已敲定」。技术选型见开发/02。任务按开发/01，T0.1–T13 已验证。
一次只做一个任务，验证过了再往下。不要改产品命令，不要改 tools-lib，不要搬 archive/cli。
文案中文硬编码，注释写 i18n key。始终中文。
```

开场后只打开这些，不要扫整个 docs：

1. 本文 §0、§4
2. [脚手架设计 README「已敲定」](./脚手架设计/README.md)
3. [开发 README](./开发/README.md) → [01-实现顺序](./开发/01-实现顺序.md) **T0.1–T13 已验证**
4. 该任务「规格」列里的文档 + [00](./开发/00-文件分类.md) + [02](./开发/02-技术选型.md)

改规格前先对「已敲定」。覆盖不了真实场景 = 改命令文档，**不要改真实场景**。

---

## 2. 做到哪了

| 层 | 状态 |
|----|------|
| 产品规格（ARCHITECTURE / PHASE / 场景 / COMMANDS） | 现行真源已齐。合集旧稿已进 archive |
| 开发计划 | 00 职责 + 02 选型 + 01 任务清单（T0.1–T13，步步验证） |
| 环境、tools-lib 怎么接 | [07](./脚手架设计/ARCHITECTURE/07-环境.md) |
| 发布前拦 prod | [开发 README](./开发/README.md)「已锁定」 |
| `packages/cli` 按新规格重写 | T0.1–T13 已验证。旧实现只在 [archive/cli](../archive/cli/00-备份说明.md) |
| `packages/tools-lib` | **不要改**。本期要用的接口都已有 |

上一轮完成 T13：S1–S42 有 mock 单测或清单。dev 真网端到端**已打通**（2026-09-07）：login（Cookie 会话）→ create → `create-version --prepare` → 1.0.0 → 免费策略 → online → offline，资源 `6a9e4011…`（测试账号保留、已下架）。入口 `node test/run-all-scenarios.mjs --env dev`，只用 primary 账号；详见 [test/README](../../test/README.md)。`--env test` 真网未打（同一条链，dev 已过）。

**2026-09-07 追加（依赖签约改版 + 质量守卫，均已 dev 真网验证）**：

- `dep add` 签约规则改版：**不再区分免费/付费**。未授权时取对方**第一条启用策略**（`status===1` 且有 `policyId`）直接签，签后**不复查**直接写稿。删除错误码 `DEP_NO_FREE` / `DEP_NOT_AUTH` / `DEP_AUTH_MISSING`，新增 `DEP_NO_POLICY`（对方一条启用策略都没有才拒）。付费策略签完是待执行态（`authStatus=128`），支付在平台侧
- 修复真网 bug：平台要求 `POST /v2/contracts/batchSign` 的 **`subjects` 每项带 `subjectType:1`**（tools-lib 类型定义过期）。裸 API 已验证：跨账号「等待免费」策略、付费订阅策略（2400 分/期）签约均成功且落 `authStatus=128`
- dev 平台事实：`GET /v2/auths/resources/batchAuth/results` 是**用户级授权**，同账号资源与多数目标未签约就返回 `isAuth:true`，所以 CLI 签约分支多数情况不触发（这是平台行为，不是 bug）
- 架构守卫测试（防回归）：`tests/architecture/comments.test.ts`（每个导出函数必须有头注释）、`tests/architecture/encoding.test.ts`（禁 U+FEFF 乱码；Windows 写入路径会偷偷插 BOM，发现违规跑 `node test/strip-bom.mjs` 清理）。约定已写入 [02-技术选型](./开发/02-技术选型.md) §2
- 付费依赖真网验证脚本：`node test/verify-paid-dep.mjs`（dev，可重复跑；会新建烟测资源+对 b_465 发版后下架）
- **字段级校验对照表已重建**（2026-09-07）：`业务梳理/字段级校验对照表.md`——逐字段对照 Console 源码（带文件:行号出处）。据此修 CLI 两处：自定义属性值上限 100→140（Console 两入口自身不一致 100/140，取版本创建页 140）、属性/可选配置**名称查重**补齐（Console `disabledNames` 全局唯一，CLI 原来只查键）。改字段规则先对那张表 §9 的维护约定
- **真网验证五件套全绿**（2026-09-07，dev + primary 账号，报告在临时目录 `freelog-runtime-cli-verification/`）：
  - `test/run-all-scenarios.mjs`：主链（发版 → pull 改稿 → 1.1.0 → 上下架）+ 主题链，全过（Windows 下 pnpm spawn 已补 `shell:true`）
  - `test/verify-field-rules.mjs`：字段级 25/25（含关键未知项落定：**平台接受 140 长度属性值**，真提交+读回无截断）
  - `test/verify-paid-dep.mjs`：付费依赖直签+发版，过
  - `test/verify-commands.mjs`：**命令面 45/45**（status/update listing/policy template+set/attr set·rm/dep range·rm/option 全操作（RT001）/draft pull --version 覆盖语义/discard/bind 幂等+换绑门禁 --force+发新号/logout 拦截/未登录坏凭据）
  - `test/verify-scenarios.mjs`：**设计场景边角 28/28**（S3 重复建壳拒/S11 不存在的号拒且不写盘/S13·S19 --reuse-version 续旧底与底对不上拒/S16 改已发号描述/S17 定号冲突拒/S39 version set+换文件发新号+非主题目录拒/S41 template list/type list·search·info/status --json）
  - 据此修 CLI 真 bug 四个：
    1. `version draft description` 全局 `--file` 透传缺失（`description-draft.ts` 漏 optsWithGlobals 合并），多身份工程必现「一夹多条」
    2. `type search` / `type info` 把 argument（字符串）当 options 合并全局旗，`TypeError` 必崩
    3. `version draft pull --version` 的版本存在性校验依赖分页列表（会误杀旧号/放过空号）——改为以 `resourceVersionInfo1` 无 fileSha1 = 「没有这个版本」为准，不写盘
    4. `update-version` 缺上传环节：原实现复用稿内 sha1，本地文件删了也能发新号（违反「不准续用 sha1」铁律），且 S42「换构建产物」无法达成——补 `uploadAndAnalyze`（同文件秒传，换文件更新 sha1/filename；本地文件不在报「本地文件不在」）
  - 语义记录：`--file` 匹配的是身份的**内容文件路径**（filePath），不是 N.json 文件名；bind 后续命令要带 bind 时同一个 `--file`；bind 工程发新号时 `--file` 给真实存在的文件路径
- **依赖签约前端真源位置**：`D:\appinside\freelog-sub-modules\【1】dependencies-declarator`（独立微前端仓，**不要改**）。Console 的依赖授权组件（`FMicroAPP_Authorization`）只是壳，`isAllAuthComplete` 计算、版本范围校验（`semver.validRange` + `maxSatisfying`）、默认 `^latestVersion` 都在这里：`src/store/index.ts`（授权完成度聚合）、`src/components/dep.vue`（范围校验）、`src/components/resource-item.vue:92-99`（默认 range）、`src/api/request.ts`（batchAuth/cycleDependencyCheck 调用）。对照表 §4 已带行号

---

## 3. 文档听谁的

| 目录 | 听它什么 | 不要把它当成 |
|------|----------|--------------|
| [脚手架设计](./脚手架设计/README.md) | CLI 做什么、门禁、本地文件 | 实现目录设计 |
| [COMMANDS](./脚手架设计/COMMANDS.md) | 命令名单速查 | 字段 / 交互真源 |
| [场景/真实场景](./脚手架设计/场景/真实场景/README.md) | 人要干什么（不提命令） | 可改的需求清单 |
| [场景/场景实现](./脚手架设计/场景/场景实现/README.md) | 同一编号怎么敲。S1–S42 | 可以发明新命令的地方 |
| [开发](./开发/README.md) | 文件树、切片、实现门禁 | 第二份产品规格 |
| [业务梳理](./业务梳理/README.md) | Console + API 事实 | CLI 该怎么做（冲突听脚手架设计） |
| [00-README](./00-README.md) | 本夹怎么走 | 流程总纲 |
| [archive](../archive/README.md) | 旧稿、旧 CLI | 现行正文 |

过期、不要打开当真源：

- `docs/一期/archive/` 里除「对照旧函数」以外的一切
- 旧 CLI 交接：`docs/一期/archive/CLI交接文档.md`（2026-08-18，还在讲 `publish` / 合集 / Studio）
- `DESIGN.md`、根上旧产品单文档：不是本期 CLI 真源
- `ARCHITECTURE/05-共用管理.md`：只是跳转。工作稿真源是 `05-版本工作稿与独立命令.md`

---

## 4. 不许违反（写代码也一样）

**产品**

- 合集、F1、`import-dir`、RSS、支付、上抛、平台草稿、顶层 `dep`、`publish`、`release`、`session` / `studio`：不做、不注册
- `N.json` 只记不可变身份 + `filePath`。prod 不要写 `env`
- `N.version.json` 是未提交的下一版。成功 POST **必须删**；失败留下
- 没有上一版 → 只能 `create-version`，号写死 `1.0.0`，禁止 inherit
- 有 latest → 只能 `update-version`。拉缓存用 `version draft pull`，**没有** `update-version --prepare`
- 依赖：只看 `Resource.batchAuth` 的 `isAuth`；false 才签，取对方第一条启用策略直接签（不分免费/付费，付费签完为待执行，支付在平台侧），签后不复查直接写稿。不看合约。对方有 `baseUpcastResources`：不加
- 提交体 `baseUpcastResources` / `authExcludedItems` 恒 `[]`
- 主题 `RT001` / 插件 `RT002`：发版仅这两类 **且路径是目录** 才把**目录内容**打成临时 zip。其余类型给目录 → 失败。这两类给 `.zip` 文件 → 失败
- `filePath` 只是记录。发版必须确认本地路径或 `--file`。本地不在 **不准**续用稿里的 sha1
- CLI 不跑构建。`version set --file` 只改记录，不打 zip、不发版
- 上传中断整文件再传。解析走 `filesListInfo`（最长 120s），不用 `fileProperty`
- 签约体：`POST /v2/contracts/batchSign` 的 `subjects` 每项必须带 `subjectType:1`（平台强校验，tools-lib 类型定义过期）

**开发**

- 不改 `packages/tools-lib`。P0-D §7 待加接口（`getVersionAuthContract`、排除项、支付、`resolveResources` query）本期不调
- 不把 archive/cli 整份搬回 `packages/cli`
- 不把 `create-version` / `update-version` 写进同一个编排文件
- `commands/` 不打平台、不写盘规则；只解析参数、TTY、调一个 domain、打印
- 算定是 prod（省略 `--env`、`--env prod`/`production`、`FREELOG_ENV=prod`）：打平台失败，「prod 暂未开放，请用 `--env test` 或 `--env dev`」。**不要把默认改成 test**
- 交给 tools-lib 的 `getEnv()` 必须是 `prod` \| `test` \| `dev`（不要 `production`）。必须 `configurePlatform` 覆盖，空的 `FREELOG_ENV` 会落到 test
- POST 版本只许出现在切片 9 和 11 的 `submit.ts`

---

## 5. 下一刀：无（T13 已完成）

一期 T0.1–T13 已验证，dev 真网端到端已打通。不要再加命令、不要合并两条发行命令。要再打网：`node test/run-all-scenarios.mjs --env dev`（或 `--env test`），凭据与账号权限见 [test/README](../../test/README.md)。

当前阶段：**作者正在 Console 对照 CLI 手工点点看、实测**，会持续报 bug 让修。修 bug 纪律：

1. 先复现（能真网就真网 dev，不能就单测复现），再改代码
2. 改完必须过：`pnpm --filter "@freelog-cli/cli2" typecheck && test && build`（含注释/编码两道守卫），能真网则加跑 `node test/run-all-scenarios.mjs --env dev`
3. 改了行为/报错文案 → 同步对应「脚手架设计」规格 + 本文 §4/§7 + `使用/06-常见情况与报错.md`，不要让文档漂移

---

## 6. 环境与包

| | |
|--|--|
| 包管理 | pnpm 10，Node >= 20。CLI 构建 **tsup** |
| CLI 包名 | `@freelog-cli/cli2`，二进制 `freelog-cli`。正式名以后再定 |
| tools-lib | `@freelog-cli/tools-lib2`，CLI **只**走 `/node` 入口。**不要改这个包** |
| 根脚本（现状） | `pnpm --filter @freelog-cli/cli2 …` |
| Shell 乱码 | **Windows 是 GBK 控制台，每次 Shell 会话开头先 `chcp 65001 | Out-Null; [Console]::OutputEncoding=[System.Text.Encoding]::UTF8` 再干活**，否则中文输出/文件路径全花、grep 断言可能静默失效。写文件一律走 Write/StrReplace 工具；冒出 U+FEFF 跑 `node test/strip-bom.mjs` |

三套环境（产品默认省略 = prod；**实现先拦 prod**）：

| `--env` | API |
|---------|-----|
| `prod` | `https://api.freelog.cn` |
| `test` | `https://api.testfreelog.com` |
| `dev` | `https://api.devfreelog.com` |

凭据：工作区 `.freelog/auth` 往上找，没有才 `~/.freelog-auth`。坏文件不准回退。一份凭据一个环境。仓库和文档不写密码 / token。

Console 证据（对照，不是规格）：`D:\appinside\freelogfe-web-repos`（页面在 `packages\console\src\pages\resource`；`FUtil.Regexp` 在 `packages\@freelog\tools-lib\src\utils\regexp.ts`）  
依赖签约微前端（对照，不是规格，**不要改**）：`D:\appinside\freelog-sub-modules\【1】dependencies-declarator`

---

## 7. 已经 dig 过、不要重开的坑

| 错觉 | 现行口径 |
|------|----------|
| 一条 `publish` / `release` 走完发版 | 拆成建壳、工作稿、两条提交、`online` |
| 省略 `--env` 打 test 更安全 | 产品默认 prod；实现拦 prod，联调显式 `--env test`/`dev` |
| 空 `FREELOG_ENV` 就能对到对的环境 | tools-lib Node 默认会落到 **test**。必须 `configurePlatform({ getEnv })` |
| 主题/插件用 `artifactMode` 决定打不打 zip | 看 `typeCode` 是不是 `RT001`/`RT002` + 路径是不是目录。`N.json` 不要 `artifactMode` |
| 本地文件不在可以续用 sha1 | 禁止。发版必须有确认过的本地路径 |
| 加依赖要先看合约 / 上抛 | 只看 `isAuth`。上抛不加。提交上抛名单恒 `[]` |
| 先补 tools-lib 再写 CLI | 本期接口已有。§7 待加的本期不调 |
| `00-README.md` 还是 F1–F3 总纲 | 已收成目录入口。真源在脚手架设计 |
| 旧 citty / `t(key)` 是新包该用的 | 选型见 02。文案硬编码中文 |
| 签约必须挑免费策略、签完再查一遍 isAuth | 2026-09-07 改版：取第一条启用策略直接签（付费签完是待执行态，支付在平台侧），签后不复查直接写稿 |
| `batchSign` 的 `subjects` 只要 `{subjectId, policyId}` | 平台要求每项带 `subjectType:1`，漏了报 `params-validate-failed` |
| `batchAuth` 返回 false 才需要签约 | dev 是用户级授权，多数目标未签约就 `isAuth:true`；签约分支多数不触发是正常现象 |
| PowerShell 直接跑，输出中文照读 | **不行**。GBK 控制台会把 UTF-8 打成乱码，中文断言/路径匹配都可能静默失效。每条命令前缀 `chcp 65001 | Out-Null; [Console]::OutputEncoding=[System.Text.Encoding]::UTF8`；优先用 Read/Grep 工具替代终端读文件 |

版本四层不要搅：看 / 管缓存 / 改稿 / 提交。只有最后一层 POST 版本。

---

## 8. 切片总序（写代码时对着勾）

见 [01 进度表](./开发/01-实现顺序.md)。T0.1–T13 **已验证**。

场景编号：S1–S8 壳与首版；S9–S17 发新号；S18–S25 工作稿覆盖；S26–S35 文件属性依赖；S36–S42 主题插件。写死文案见 [场景实现 README](./脚手架设计/场景/场景实现/README.md)。

---

## 9. 改完规格或切完一片时

把本文 §0 / §2 的「做到哪」改成最新状态。不要把字段和门禁抄进本文——仍写在脚手架设计 / 开发。
