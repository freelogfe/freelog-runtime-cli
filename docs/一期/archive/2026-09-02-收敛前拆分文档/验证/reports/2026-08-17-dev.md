# 2026-08-17 dev 真实验证报告

> 文档角色：日期化运行证据。本文只记录本次执行结果，不定义产品范围或长期完成状态。

## 运行上下文

- 日期：2026-08-17（Asia/Shanghai）
- 环境：`dev`（`https://api.devfreelog.com`）
- CLI 版本：`0.5.0`
- CLI 基线 commit：`d40ade5358929445dda1bab576a5ab707a173aaf`
- Console 证据 commit：`d74121e647f0223203f1f0bb317354b4191266f1`
- 工作区：基线 commit + 本轮交接文档、package 叶子解析修复及测试 diff，尚未提交
- primary：`freelog-test11`，承担全部正常创建、发行、维护和 Vite 模板链
- secondary：`snnaenu`，只用于 Studio 多账号与跨 owner 拒绝
- 凭据来源：`test/.freelog-test-credentials.local.json`；密码集中记录在
  [CLI交接文档 §3.2](../../CLI交接文档.md#32-dev-两个联调账号)，本文不复制

## 范围裁决

- 正式测试范围：独立资源、合集、批量、草稿、策略、上下架、依赖授权、session/Studio、
  Console parity 和四套 Vite runtime 模板。
- Webpack 已删除，不测试。
- **package 按项目负责人 2026-08-17 指令暂停测试，不计入本轮正式验收。**暂停指令到达前
  已发生的 package 探索运行只记录在“探索发现”，不得算作签字。
- prod 不在本次范围。

## 正式结果

| 验证层 | 结果 |
|---|---|
| 最终离线 `verify` | PASS：76 个测试文件、442 项测试；typecheck、i18n、7 套兼容清单、build、npm pack dry-run 全部通过 |
| dev 主场景 S1-S16 | PASS：133/133，0 skip，0 fail |
| negative gates | PASS：9；SKIP：1（NEG-10 frozen fixture） |
| batch boundary | PASS：4/4 |
| JSON envelope | PASS：12/12 |
| chaos | PASS：4/4 |
| Console parity 子脚本 | PASS：10/10 |
| session smoke | PASS：13/13 |
| P6 parity | PASS：6；SKIP：1（P6-4 frozen fixture） |
| L3-H session/Studio | PASS：4/4，0 skip；双账号 owner 与非 owner 门禁通过 |
| Vite runtime 首发 | PASS：4/4；均完成 init、install、build、create、publish、策略和 online |
| Vite runtime 更新版本 | PASS：React TS、Vue TS 均从 1.0.0 更新到 1.0.1 |

主 runner 报告：`%TEMP%/freelog-runtime-cli-verification/latest.txt`。

## primary 真实业务覆盖

`freelog-test11` 实际完成：

- 图片、视频、主题 zip、小说的创建、首版发行、发新版、版本说明与属性维护；
- 资源基础信息、封面、标签、pull、bind 和 owner 校验；
- 图片合集、视频合集、小说合集的目录导入、排序、删除、发版、目录 merge 和属性同步；
- 批量独立资源、21 文件分批边界、失败报告 resume/retry 与配置漂移门禁；
- 免费策略新增、启停、最后启用策略门禁、上下架；
- 依赖增删改、版本范围、同 owner 依赖授权；
- 独立资源和合集版本草稿 push/pull/discard；
- JSON envelope、debug 脱敏、中文与空格路径、重复版本、远端结果未知恢复门禁；
- session 首发、维护、导出工程；Studio primary 子工程发行。

`snnaenu` 只用于 secondary Studio 子工程和对 primary 资源的非 owner 拒绝，之后恢复 primary。

## Vite runtime 远端证据

| 模板 | 资源 ID | owner | latest | status | 启用策略 |
|---|---|---|---:|---:|---:|
| `vite-react-ts` | `6a8277fff5749f00306eab73` | `freelog-test11` | `1.0.1` | 1 | 1 |
| `vite-react` | `6a82780cf5749f00306eabb3` | `freelog-test11` | `1.0.0` | 1 | 1 |
| `vite-vue-ts` | `6a82781df5749f00306eabf7` | `freelog-test11` | `1.0.1` | 1 | 1 |
| `vite-vue` | `6a82782af5749f00306eac37` | `freelog-test11` | `1.0.0` | 1 | 1 |

四套工程均在系统临时目录生成，没有写回仓库；状态读回确认 owner 匹配、正式版本存在、资源已
上架且有一条启用策略。

## L3-H 远端证据

| 场景 | 资源 ID | 说明 |
|---|---|---|
| H1 session | `6a82778df5749f00306eaad8` | primary 无 Store 首发 |
| H3 Studio primary | `6a82778ef5749f00306eab19` | owner=`freelog-test11` |
| H3 Studio secondary | `6a82778ff5749f00306eab3b` | owner=`snnaenu` |

机器可读结果：`L3H_RESULT` 为 `pass`、4 passed、0 skipped。

## 探索发现：package 父类型

暂停指令到达前，package 实际链发现 `init package` 把父节点 `RT029/前端库` 写入 manifest，
平台 create 返回“资源类型不存在、创建失败或非叶子类型”。证据：

- dev `type info RT029`：`isTerminate=false`；
- dev 叶子：`RT029001/JS工具包`、`RT029002/组件库`，均 `isTerminate=true`；
- Console `FResourceTypeInput4` 搜索接口固定传 `isTerminate: true`。

本轮已先更新设计，再修正为按模板展示名解析当前环境叶子 code，并增加 2 项回归测试。暂停
指令前的探索性复测已确认三个 package 模板可 create/publish/online，但按负责人裁决，
**这些结果不计入本报告正式验收，后续不再继续 package 测试。**

## 未签字项

| 项 | 状态 | 原因与补齐方式 |
|---|---|---|
| frozen NEG-10 / P6-4 | SKIP | dev 普通账号不能通过 API 设置冻结；需 Console 预置 primary 自有冻结资源 ID，写入 ignored fixture 后重跑 |
| RSS | 未执行 | 需要受控 feed、owner 邮箱和一次性验证码；账号密码不足以完成两阶段 prepare/complete |
| package | 暂停 | 2026-08-17 项目负责人明确暂停测试，等待重新开启范围 |
| prod | 硬禁用 | 当前 CLI 在 API/Console URL 解析和平台写入前以 code 4 停止，不执行 prod smoke |

## 结论

当前批准范围内没有真实 dev 失败项。primary 正常业务链、双账号隔离、四套 Vite runtime 首发
和代表性更新版本均通过。frozen、RSS 和 package 必须保持各自的 SKIP/未执行/暂停状态，
不得写成 PASS。

## 同日追加：官方使用文档交付回归

本轮只验证公开文档和 CLI 命令表面，未执行新的 dev/prod 远端写操作，也未恢复 package 业务测试：

| 检查 | 结果 |
|---|---|
| 全量单元测试 | PASS：78 个测试文件、450 项测试 |
| 文档治理 | PASS：公开目录自包含、相对链接、侧栏顺序、敏感信息和发布范围检查通过 |
| 文档命令契约 | PASS：所有代码块中的 `freelog-cli` 命令均已挂载，flag 均有声明 |
| 公开命令表面 | PASS：默认 help 隐藏 `cover` / `meta`；`FREELOG_DEV=1` 时可用 |
| dep 会话参数 | PASS：`dep add` help 含 Store 会话参数，不含无关 reuse 参数 |
| typecheck / build | PASS |
| npm pack dry-run | PASS：`@freelog-cli/cli2@0.5.0`，11 个文件 |

npm 远端发布门禁仍未满足：当前包名 `@freelog-cli/cli2` 是为了不覆盖线上 `@freelog-cli/cli` 而采用的
隔离测试包名，不代表最终正式包名；它尚未完成目标 dist-tag 验证，并且
production 已硬禁用。必须先解除 production 门禁、完成独立上线验收并确认目标包 `latest` 后，
才能把新使用文档作为正式环境教程上线。单元测试中覆盖共享 scaffold 代码不代表 package 业务
流程恢复验收，package 状态仍为暂停。

## 同日追加：production 硬门禁

本轮未发起 production 网络请求。门禁与可发现性验证结果：

| 检查 | 结果 |
|---|---|
| `type list --env prod --json` | code 4，`cli.production_env_disabled` |
| API / Console URL 解析 | 单元测试确认 production 均在返回 URL 前失败 |
| tools-lib2 Node adapter | `getEnv` 返回前执行同一门禁 |
| `config set --default-env prod` | code 4，不写入项目配置 |
| shell 环境补全值 | 仅 `dev test`，不暴露 `prod production` |
| prod smoke | package script 与执行脚本已移除；历史报告仅保留为历史记录 |

## 同日追加：runtime 模板 latest 发布门禁

本轮只完成模板和 CLI 的本地改造及发布前检查，没有执行 npm 发布，也没有恢复 package 业务测试。

| 检查 | 结果 |
|---|---|
| 四套 runtime 模板本地版本 | `4.0.0` |
| 四套模板 `npm pack --dry-run --json` | PASS：均包含 `template.manifest.json` 和 `template/` |
| CLI 本地/link 初始化 | 使用仓库内模板，并回报实际模板版本 `4.0.0` |
| CLI 构建产物 `init theme` 冒烟 | PASS：JSON 回报 `@freelog-cli/template-vite-react-ts@4.0.0` |
| CLI 正式安装版初始化 | 查询 npm `latest`，解析为具体 SemVer 后下载、校验并缓存 |
| npm 当前 `latest` | 四套均为旧 `3.0.0`，早于待发布的本地 `4.0.0` |
| `verify:template-registry` | 预期 FAIL：4 项，阻止在模板发布前先发布 `cli2` |

发布顺序固定为：先发布四套 runtime 模板 `4.0.0`，确认四个 npm `latest` tarball 通过
`verify:template-registry`，再发布 `@freelog-cli/cli2`。package 模板仍固定为 `0.5.0` 且暂停验收，
不参与本次 latest 切换。

## 同日追加：私有 tools-lib 随 CLI 内联

`packages/tools-lib` 是 CLI 的私有 workspace 构建依赖，不发布到 npm。CLI 的发布 tarball 不声明
`@freelog-cli/tools-lib2` 运行时依赖；构建配置强制内联其 Node adapter 和传递依赖，postbuild 会阻止
私有 import 或公开声明引用重新进入 `dist`。

| 检查 | 结果 |
|---|---|
| workspace 依赖 | PASS：CLI 使用 `workspace:^` devDependency，lockfile 指向 `packages/tools-lib` |
| 隔离安装 | PASS：全新临时目录安装 CLI tarball，不存在私有 tools-lib，`freelog-cli --help` 可运行 |
| 发布前构建顺序 | PASS：CLI `prepublishOnly` 先构建 tools-lib，再执行 CLI 完整验证和模板注册表门禁 |

## 同日追加：npm 模板发布与 CLI 安装验证

本轮使用 npm 公共 registry 验证，不执行 dev/prod 平台写操作。四套 runtime 模板与 CLI 均已发布；
本节区分本地 tarball 验证与最终 npm 全局安装验证。

| 检查 | 结果 |
|---|---|
| runtime 模板 npm `latest` | PASS：`template-vite-react`、`template-vite-react-ts`、`template-vite-vue`、`template-vite-vue-ts` 均为 `4.0.0` |
| 模板注册表门禁 | PASS：`pnpm --filter @freelog-cli/cli2 verify:template-registry` 通过 4 套模板 |
| CLI tarball 隔离安装 | PASS：全新临时目录 `npm install` 后，`freelog-cli --version` 返回 `0.5.0`，`--help` 与 `init --help` 可用 |
| 安装版 npm 模板初始化 | PASS：不传本地模板目录，四套 `init theme --template vite-* --runtime 0.5 --skip-install` 均解析并下载对应 `@freelog-cli/template-vite-*@4.0.0` |
| 四套工程构建 | PASS：React、React TS、Vue、Vue TS 均完成 `npm install` 与 `npm run build`，生成 `dist/` |
| `@freelog-cli/cli2@latest` 公共查询 | PASS：npm registry HTTP 200，匿名查询与 dist-tag 均返回 `0.5.0` |
| `npm install --global @freelog-cli/cli2@latest` | PASS：隔离全局前缀安装完成，`freelog-cli --version` 返回 `0.5.0` |
| npm CLI 模板初始化 | PASS：已安装 CLI 成功执行 `init theme --template vite-vue-ts --runtime 0.5`，解析线上模板 `4.0.0` |

临时安装验证目录位于 `%TEMP%/freelog-cli2-install-smoke-cf5a68c64a58425391d979f90e86e297` 与
`%TEMP%/freelog-cli2-global-smoke-254869f023f647d9a92428a7ed2f8606`，不属于仓库，也不含平台凭据。

## 同日追加：已发布 cli2 的真实 dev 回归

本轮先用公开 npm 的 `@freelog-cli/cli2@0.5.0` 做隔离全局安装，再以 primary 在 dev 完成一条发布版
主题链路；随后运行仓库全场景入口。未执行 production 写入，package 业务仍按范围裁决暂停。

| 检查 | 结果 |
|---|---|
| 发布版 npm CLI | PASS：隔离全局安装 `0.5.0`，工作区凭据登录 primary，线上 `vite-react-ts@4.0.0` 初始化、`npm install`、构建、create、`1.0.0` publish、策略、online、listing update、`1.0.1` publish 与 status 回读均通过 |
| 发布版资源证据 | PASS：`6a82bc5df5749f00306ee1bc`，owner=`freelog-test11`，latest=`1.0.1`，status=`1`，enabledPolicyCount=`1` |
| `test/run-all-scenarios.mjs --env dev` | PASS：主场景 132 通过、1 跳过、0 失败；NEG 9 通过、1 frozen fixture 跳过；BATCH 4/4、JSON 12/12、CHAOS 4/4 |
| Console parity | PASS：10 个子脚本全部通过，包含 createVersion、合集更新、属性同步、batch、meta 与封面同步/SSE 一致性 |
| package 范围 | SKIP：`S8b init package` 明确不计入本轮 dev 签字；不是 PASS，也不是失败 |

本轮发现并修复两项验证编排问题：

1. 非交互 `type pick --category package` 没有模板上下文，面对「JS工具包」和「组件库」必须 code 4 拒绝；
   runner 原先错误地期待任选一个叶子，现改为验证该拒绝门禁。
2. `cover` 是隐藏的研发 parity 命令，`verify-cover-parity.mjs` 现只为自身启动的子 CLI 传入
   `FREELOG_DEV=1`，公开安装版默认命令面保持不变。
