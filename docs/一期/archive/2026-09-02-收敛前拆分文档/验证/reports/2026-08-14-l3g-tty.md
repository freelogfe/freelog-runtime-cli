# 2026-08-14 L3-G TTY 字段约束 — 验收报告

> 文档角色：L3-G 运行证据（自动化层 + 校验器同源证明）。交互式 @clack 向导 UX（G2-1/G2-3）须在真实 TTY 补签。

## 1. 运行上下文

| 项 | 值 |
|---|---|
| 日期 | 2026-08-14 |
| 环境 | `dev`（G5 使用 `--env dev`；无平台写操作） |
| 执行人 | Cursor Agent（自动化验收） |
| CLI 路径 | `node packages/cli/dist/bin/index.js` |
| CLI commit | `f77c48237b612af2e796d123028fc31f1e720522` |
| Console 对照 commit | `d74121e647f0223203f1f0bb317354b4191266f1` |
| 终端 | 非 TTY（CI 等价层）+ 单测/CLI 探针 |
| 验收脚本 | `pnpm --filter @freelog-cli/cli verify:l3g-automated` |

## 2. 自动化预检

| 命令 | 结果 |
|---|---|
| TTY 单测（fieldConstraints + preflightSummary + onlineGates + onlineService） | **PASS 22/22** |
| `verify:console-forms` | **PASS 21/21** |
| `verify:l3g-automated` | **PASS 17/17 可自动化项；5 跳过** |

## 3. 逐条验收

**图例：** ✅通过 ⏭跳过（需真实 TTY / dev 登录 / RSS fixture）

### G1 — init / import / collection folder（P1）

| # | 操作 | 结果 | 证据 |
|---|---|---|---|
| G1-1 | init 标题 101 字 | ✅ | `fieldConstraints.test.ts` + `verify:l3g-automated` |
| G1-2 | 授权标识 `My theme@$#` | ✅ | 规范化 `My_theme_`；`resourceName.test.ts` 同源 Console |
| G1-3 | 授权标识 `/` 或 61 字 | ✅ | `fieldConstraints.test.ts` |
| G1-4 | hint message | ✅ | bundled `rqr_input_resourceauthid_hint` 含 1–60 / emoji |
| G1-5 | import-dir 标题前缀 101 字 | ✅ | `FIELD_SPECS['FORM-BATCH-TITLE']` |
| G1-6 | 标题前缀空 | ✅ | `fieldConstraints.test.ts` |
| G1-7 | collection 合集标题 101 字 | ✅ | 与 G1-1 同 `clackTextField('FORM-RES-TITLE')` |
| G1-8 | collection 短名 `foo bar` | ✅ | 与 G1-2 同 `normalizePromptCreateName` |

**Console 并排（G1-2）：**

| 项 | Console | CLI | 一致 |
|---|---|---|---|
| `My theme@$#` → 最终标识 | `resourceNameOptimized` → `_` | `My_theme_` | ✅ |
| 自动转换提示 | `input_resourceauthid_automodified_msg` | `consola.info` 同 key | ✅（代码路径） |

### G2 — 旗标 TTY 向导（P2）

| # | 操作 | 结果 | 说明 |
|---|---|---|---|
| G2-1 | `create` 向导 UX | ⏭ | 需真实 TTY 多步 pick；`shouldRunFieldWizard` + `createWizard.ts` 已接入 |
| G2-2 | `update` intro 201 字 | ✅ | `FIELD_SPECS['FORM-LIST-INTRO']` / `validation.test.ts` |
| G2-3 | `collection update` 向导 UX | ⏭ | 需真实 TTY；`collectionUpdateWizard.ts` 已接入 |
| G2-4 | RSS 托管 update | ⏭ | 无 dev RSS fixture |

### G3 — confirm 前 preflight（P3）

| # | 操作 | 结果 | 证据 |
|---|---|---|---|
| G3-1 | `online` gates 摘要 | ✅ | `preflightSummary.test.ts` 三分支 + `onlineService.test.ts` |
| G3-2 | `collection publish` preflight | ✅ | `collection/publish.ts` 调用 `summarizePublishPreflight` |
| G3-3 | `draft push --force` preflight | ✅ | `draft.ts` 调用 `summarizePublishPreflight` |

### G4 — --help / 动态提示（P4/P5）

| # | 操作 | 结果 | 证据 |
|---|---|---|---|
| G4-1 | `create --help` | ✅ | 含「100」「非空」等约束摘要 |
| G4-2 | `update --help` | ✅ | intro 含「200」 |
| G4-3 | `version set --file` | ⏭ | 需 dev login + 类型 API |
| G4-4 | `publish` 文件 hint | ⏭ | 需已登录工程 + filePath |

### G5 — 非 TTY 回归

| # | 操作 | 结果 | 证据 |
|---|---|---|---|
| G5-1 | `create --yes` 缺 flag | ✅ | exit 4 |
| G5-2 | `update --yes` 无 listing | ✅ | exit 4 |

## 4. 计划内边界（已确认，不计 FAIL）

- `FORM-POL-NAME` 无独立 TTY prompt
- `resource publish` 无 confirm，仅有 TTY 文件 hint
- RSS ENV / G2-4 未在本报告覆盖

## 5. 差异记录

无阻塞差异。

## 6. 分组结论

| 分组 | 通过/总 | 结论 |
|---|---|---|
| G1 P1 | 8/8 | ✅通过 |
| G2 P2 | 1/4 自动化 + 2/4 跳过 UX + 1/4 跳过 RSS | ✅通过（自动化层） |
| G3 P3 | 3/3 | ✅通过 |
| G4 P4/P5 | 2/4 + 2 跳过 | ✅通过（自动化层） |
| G5 回归 | 2/2 | ✅通过 |
| **Console Step1/Step4 一致** | G1-2 | ✅是（resourceNameOptimized 同源） |

## 7. 总签字

| 检查人 | 日期 | L3-G 总结论 | 关联 dev 报告 |
|---|---|---|---|
| Cursor Agent（自动化） | 2026-08-14 | ✅ **PASS（自动化层 17/17）**；⏭ G2-1/G2-3/G2-4/G4-3/G4-4 待真实 TTY/dev 补签 | [2026-08-14-dev.md](./2026-08-14-dev.md) |

**签字条件：** §2 预检 PASS ✅；§5 无阻塞差异 ✅；G1-2 Console 同源 ✅。

---

*生成：`pnpm --filter @freelog-cli/cli verify:l3g-automated` · 结果 JSON：`packages/cli/scripts/verify-l3g-automated.result.json`*
