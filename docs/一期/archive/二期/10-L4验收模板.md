# L4 展品 smoke 验收模板（草案）

> 文档角色：二期 dev E2E 人工/自动化报告模板。对应 [04-能力矩阵与验收 §9](./04-能力矩阵与验收.md#9-二期-mvp-验收链verifyexhibit-smoke--规划)。**规划脚本** `pnpm verify:exhibit-smoke`（未实现）。Console 并排步骤见 [05 §C.1](./05-Console源码证据与调用链.md#c1-core-追溯矩阵)。

环境：`--env <env>` · 测试日期：________ · Console commit：`d74121e647f0223203f1f0bb317354b4191266f1`（或实测值）· CLI 版本：________

## 前置

- [ ] 节点已在 **Console** 创建（CLI 不创建节点）
- [ ] `NODE_ID=________`
- [ ] 凭据已配置（gitignore 本地文件或 CI secret）
- [ ] `RESOURCE_ID=________`（市场可签约或自发行已 online）
- [ ] 已读 [12-多视角设计说明 §4](./12-多视角设计说明.md#4-测试视角)（验收层 B + C）

## S1 — market search（NM-01 · T-NM-S1）

```bash
freelog-cli market search --limit 5 --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 成功 | [ ] `ok: true`；`data.items[].resourceId` 非空 | [ ] 发现页市场 Tab 列表有项；Network `Resource.list` `status:1` |

## S2 — market show（NM-02 · T-NM-S2）

```bash
freelog-cli market show <RESOURCE_ID> --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 可签约 | [ ] `status === 1`；`canSignToNode` 或 policies 非空 | [ ] 详情页可点「签约至节点」；blockers 与 CLI 一致 |

## S3 — node show（NM-04 · T-NM-S3）

```bash
freelog-cli node show <NODE_ID> --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 门禁 | [ ] owner 匹配；`frozen === false` | [ ] 节点管理页正常进入；非 owner/冻结与 CLI code 2 同类 |

## S4 — exhibit sign dry-run（NM-08 · T-NM-S4）

```bash
freelog-cli node exhibit sign --node <NODE_ID> --resource <RESOURCE_ID> --dry-run --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 预检 | [ ] checks pass 或预期 warn | [ ] Drawer 选同一节点；预检通过或黄标 warning（未提交） |

## S5 — exhibit sign（NM-08 · T-NM-S5）

```bash
freelog-cli node exhibit sign --node <NODE_ID> --resource <RESOURCE_ID> --yes --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 创建展品 | [ ] `presentableId=________` | [ ] 展品 Tab 出现新行；Network `batchCreatePresentable` 200 |

## S6 — exhibit online（NM-11 · T-NM-S6）

```bash
freelog-cli node exhibit online <PRESENTABLE_ID> --yes --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 上架 | [ ] `onlineStatus === 1` | [ ] 展品行在线；Network `presentablesOnlineStatus` → 1 |

## S7 — exhibit list（NM-09 · T-NM-S7）

```bash
freelog-cli node exhibit list --node <NODE_ID> --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 列表 | [ ] 含 S5 的 presentableId 且在线 | [ ] 展品 Tab 筛「在线」含同一 presentable |

## S8 — exhibit offline（NM-12 · T-NM-S8）

```bash
freelog-cli node exhibit offline <PRESENTABLE_ID> --yes --json --env <env>
```

| 检查项 | CLI | Console 并排 |
|---|---|---|
| 下架 | [ ] `onlineStatus === 0` | [ ] 展品行离线；Network `presentablesOnlineStatus` → 0 |

## 负向（可选 · 层 C 建议至少 N1 + N4）

| ID | 场景 | CLI 期望 | Console 并排期望 |
|---|---|:---:|---|
| N1 | 非 owner | code **2** | 账号 B 打开节点 URL → 403 / 无管理 |
| N2 | 冻结节点 | code **2** | 管理页拦截 |
| N3 | 重复 sign | code **4** | Drawer 提示已签约 |
| N4 | 无 policy online | code **4** | Console 弹 policy 窗；CLI 不弹窗 |

## Handoff（code 5 · T-H5）

适用：付费 policy、`sign` 返回 `error.code === 5`。

| 步骤 | 操作 | 记录 |
|:---:|---|---|
| 1 | 运行 `node exhibit sign ... --json`，保存完整 JSON | [ ] |
| 2 | 确认 `error.reason` 为 `payment_required`（或等价） | [ ] |
| 3 | 在浏览器打开 `error.contractsUrl` 或 `actionUrl` | [ ] |
| 4 | 在 Console 完成合约/收银/授权 | [ ] |
| 5 | 复制 JSON 中的 `error.nextCommand`，在终端重跑 | [ ] |
| 6 | 断言 `ok: true` 且 `data.presentableId` 非空 | [ ] presentableId=________ |

自动化脚本 **不** 覆盖 T-H5；见 [16 §4](./16-verify-exhibit-smoke设计.md#4-步骤映射)。

Fixture 准备见 [16 §6](./16-verify-exhibit-smoke设计.md#6-fixture-准备手册附录)。

## 签字

| 检查人 | 日期 | CLI 版本 | Console commit | 验收层 | 总评 |
|---|---|---|---|:---:|---|
| | | | | □B □B+C | □通过 □差异 □阻塞 |
