# 2026-08-14 dev 验证报告

> 文档角色：日期化运行证据。本文只记录本次执行结果，不定义产品范围或长期完成状态。

## 运行上下文

- 日期：2026-08-14
- 环境：`dev`（`https://api.devfreelog.com`）
- CLI commit：`f77c48237b612af2e796d123028fc31f1e720522`
- Console commit：`d74121e647f0223203f1f0bb317354b4191266f1`（能力矩阵证据快照；未在本轮重跑 `verify:console-forms`）
- 账号：本机 dev 凭据（`.freelog-auth` 加密落盘；未写入本文）

## 结果

| 验证层 | 命令 | 结果 |
|---|---|---|
| 全量 CI | `pnpm verify` | PASS：**368** 测试 + typecheck + build |
| 会话 MVP | `pnpm --filter @freelog-cli/cli verify:session-smoke` | PASS：**13/13**（含 SES-02 draft `--session` code 4） |
| P6 parity | `pnpm --filter @freelog-cli/cli verify:p6-parity` | PASS：**6 pass**；**1 skip**（P6-4 frozen，无 fixture） |
| 文档治理 | `documentationGovernance.test.ts` | PASS：**11/11**（含 DESIGN 凭据加密 write/decrypt 契约） |

## 本轮相对 2026-08-12 的增量

- **凭据加密**：`login` 写入 AES-256-GCM；`~/.freelog-cli/auth.key` 默认主密钥；读取路径解密后使用（`authAndDebug.test.ts`）
- **draft 会话门禁**：`draft * --session` → code 4，不触达 draft API（`draftSession.test.ts`）
- **双模式 + P6**：reuse-version、dep 默认 range、N-06 会话 smoke 与文档对齐（见 `28bb805` / `f77c482`）

## 未进入本次 ENV 结论（须单独记录）

| 项 | 状态 | 补齐方式 |
|---|---|---|
| **P6-4 / NEG-10 frozen** | SKIP | Console 手动冻结资源 → `test/.freelog-test-fixtures.local.json` 或 `FREELOG_TEST_FROZEN_RESOURCE_ID`；`pnpm provision:frozen-fixture` 验证 |
| **L1 全场景** | 未在本轮重跑 | `node test/run-all-scenarios.mjs --env dev` |
| **L3-G TTY 字段约束** | 自动化 ✅ **17/17**；TTY UX 5 项 ⏭ | [2026-08-14-l3g-tty.md](./2026-08-14-l3g-tty.md)；`pnpm verify:l3g-automated` |
| **RSS C-10** | 未在本轮重跑 | `verify:rss` + 受控 feed 人工链 |
| **prod** | 无本次签字 | `verify-prod-smoke` + prod 报告模板 |

## 追加：TTY 字段约束（P0–P5，同日二次 verify）

| 验证层 | 命令 | 结果 |
|---|---|---|
| TTY 单测 | `fieldConstraints` + `preflightSummary` + `onlineGates` + `onlineService` | PASS：**22/22** |
| 全量 CI | `pnpm verify` | PASS：**382** 测试 + typecheck + build |
| Console 表单锚点 | `verify:console-forms` | PASS：**21/21** |
| 文档治理 | `documentationGovernance.test.ts` | PASS：**13/13** |

实现规格：[CLI交互与字段约束](../开发/CLI交互与字段约束.md)；代码真源 `services/shared/fieldConstraints.ts`。

## L3-G 人工验收

> 完整报告：[2026-08-14-l3g-tty.md](./2026-08-14-l3g-tty.md)（自动化层 **PASS**）。补签：真实 TTY 下 G2-1/G2-3 向导 UX、G2-4 RSS、G4-3/G4-4（需 dev 登录工程）。

| 分组 | 通过/总 | 结论 | 检查人 | 日期 |
|---|---|---|---|---|
| G1 P1 | 8/8 | ✅通过 | Cursor Agent | 2026-08-14 |
| G2 P2 | 1/4 + 3⏭ | ✅自动化层 | Cursor Agent | 2026-08-14 |
| G3 P3 | 3/3 | ✅通过 | Cursor Agent | 2026-08-14 |
| G4 P4/P5 | 2/4 + 2⏭ | ✅自动化层 | Cursor Agent | 2026-08-14 |
| G5 回归 | 2/2 | ✅通过 | Cursor Agent | 2026-08-14 |
| Console Step1/Step4（G1-2） | — | ✅是 | Cursor Agent | 2026-08-14 |
| **L3-G 总结论** | — | ✅ **PASS（自动化层）** | Cursor Agent | 2026-08-14 |

阻塞差异：无。

复跑：`pnpm --filter @freelog-cli/cli verify:l3g-automated`

## 追加：一致性审查修复（同日工作树）

- 基线 commit：`6afb41fad6ef8ddc48be57d5bb5d5cbd09d400e7`；结果对应其上的未提交工作树。
- 修复范围：createBatch 未知结果禁止自动降级、工程 Store revision 重入、命令会话依赖导出契约、合集在线门禁与 100 项分批、Studio 对账远端身份/SHA1 校验、已发版 videoCover 边界、跨平台幂等键与目录 fsync。
- 全量验证：`pnpm --filter @freelog-cli/cli verify` PASS；**76** 个测试文件、**439** 项测试，typecheck、i18n audit、**7** 个当前模板兼容检查、build、npm pack dry-run 全部通过。
- 静态质量：`git diff --check` PASS（仅 Git 的 LF→CRLF 提示，无 whitespace error）。
- 本节不包含真实 dev 平台写入、真实 TTY、RSS、冻结 fixture 或 production 签字；不得把离线 verify 等同 ENV/人工验收。

## 数据说明

脚本在 dev 创建测试资源；本地临时目录会清理，远端资源无统一回收协议。勿将写操作脚本指向 production。

## 追加：真实 dev 第二阶段（进行中）

- L3-H 真实集成：PASS，4/4、0 skip；覆盖 session 首发与导出、双账号 Studio 首发、非 owner 门禁。机器可读证据见 `2026-08-14-l3h-real.json`。
- `run-all-scenarios` 首轮：FAIL，68 pass、0 skip、10 fail。失败集中在普通工程 `create --yes`：命令错误地要求重复传 `--title/--type/--name`，合集与批量链路正常。
- 裁决：工程 `create` 使用 `flags > manifest`；完整 manifest 可直接创建。修复后必须从失败的普通资源主链重新执行，不把首轮失败记为通过。
- 最小复测继续发现 payload 来源问题：manifest 标准类型展示名被误当成显式 `--type-name`，平台按自定义子类型拒绝。裁决为标准 `RT*` 省略 `resourceTypeName`，仅显式自定义意图提交。

### 修复后结论

| 验证层 | 结果 |
|---|---|
| L3-H 真实 session / Studio | PASS：4/4，0 skip；含双账号与非 owner 门禁 |
| `run-all-scenarios` 主场景 | PASS：133/133，0 skip |
| L2 negative gates | PASS：9；1 skip（缺 frozen fixture） |
| L2 batch / JSON / chaos | PASS：4/4、12/12、4/4 |
| Console parity 子脚本 | PASS：10 个子脚本全部通过 |
| session smoke | PASS：13/13 |
| P6 parity | PASS：6；1 skip（缺 frozen fixture） |
| 最终离线 `verify` | PASS：76 个测试文件、440 项测试；typecheck、i18n、7 个当前模板兼容、build、npm pack 全部通过 |

本轮真实环境确认了普通图片、视频、主题 zip、小说、图片/视频/小说合集、批量独立资源、
基础信息更新、发新版、已发版说明/属性维护、草稿、策略增删启停、上下架、依赖授权、bind、
跨账号 owner、session 与 Studio。首次全场景暴露的工程 `create` 字段解析和类型名来源问题已
修复，并由第二次完整运行证明没有停留在最小复现层。

仍未签字的两项均需要仓库外 fixture：

- frozen：dev 普通账号不能通过 API 设置冻结状态，需要 Console 中预置自有冻结资源 ID；
- RSS：需要受控 feed、feed owner 邮箱及一次性验证码，账号密码本身不足以完成绑定链。

二者保持 `SKIP/未执行`，不得计入完整产品签字。全场景文本报告位于本机
`%TEMP%/freelog-runtime-cli-verification/latest.txt`；L3-H 机器可读证据见
`2026-08-14-l3h-real.json`。

### 真实模板链

- `vite-react-ts` 主题：真实 pnpm install、TypeScript/Vite build、create、release 1.0.0 PASS。
- `vite-vue-ts` 插件：真实 pnpm install、vue-tsc/Vite build、create、release 1.0.0 PASS。
- 审查发现模板依赖分类缺陷：四个 Vite 模板把类型包/构建插件放在 production dependencies；
  React TS 模板还包含未使用的 `freelog-type`，引入 Egg 2 及弃用传递依赖。该问题进入本轮
  修复并要求重新安装构建验证。
- Webpack 模板已由仓库维护者移除，不属于当前 CLI 支持面；兼容矩阵、CLI 可选列表和校验门禁
  已同步收口，避免暴露无法初始化的模板。
- Vite 清理后重新生成并安装构建：`vite-react-ts`、`vite-vue-ts` 均 PASS；生产依赖只保留
  Freelog runtime 与框架运行库，类型包/Vite 插件归入 devDependencies，React 模板不再
  安装 `freelog-type` 或 Egg 传递依赖。`check:compat` 已加入 Vite 依赖分组回归门禁。
