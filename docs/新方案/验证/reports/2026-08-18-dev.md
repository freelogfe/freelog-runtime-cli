# 2026-08-18 dev 验证报告

> 文档角色：日期化运行证据。本文只记录本次执行结果，不定义产品范围或长期完成状态。

## 运行上下文

- 日期：2026-08-18（Asia/Shanghai）
- 环境：`dev`
- CLI 版本：`0.5.0`
- 基线 commit：`95100975e8ab4bbd592c6288d2fe803b543add65`
- 当前工作区：基线 commit + 本轮未提交实现/文档 diff
- 工作区 diff Git blob 标识（排除本报告自身）：`3f70dbca68345818c8ef8d1a5d550d221a470d90`
- Console 证据 commit：`d74121e647f0223203f1f0bb317354b4191266f1`
- 打包入口 SHA256：`1EBDFF78BD3F765AAB333DA013D36E8CFF5A7973210913EAB98C35B8294DF673`
- 凭据：ignored 本地凭据文件，通过 stdin 登录；报告不记录密码、token 或 cookie

## 已通过

| 验证层 | 结果 |
|---|---|
| 发布门禁 `pnpm verify` | PASS：85 个测试文件、493 项测试；tools-lib typecheck/build、CLI typecheck、i18n、7 套模板兼容、build、真实 dist smoke、npm pack dry-run 全部通过 |
| 打包 CLI | PASS：实际 `dist/bin/index.js` 的 version/help/completion/status JSON；5 个机器 schema 均进入 npm 包 |
| dev 主场景 | PASS：132 通过、1 跳过、0 失败；独立资源、合集、批量、草稿、策略、上下架、依赖、bind 和多资源类型主链通过 |
| 负向/边界 | PASS：NEG 9、BATCH 4、JSON 12；frozen 1 项因缺夹具跳过 |
| chaos | PASS：4/4；同版本且完整发布意图一致时幂等恢复，中文/空格路径和报告恢复通过 |
| Console parity | PASS：10 个真实 dev 子脚本；createVersion、合集、属性、batch、封面同步/SSE、payload/meta 对照通过 |
| Console 表单源码契约 | PASS：21/21，绑定上述 Console commit |
| session smoke | PASS：13/13 |
| P6 | PASS：6；SKIP：1（frozen fixture） |
| L3-H | PASS：打包 CLI smoke + 4/4 source-service integration，0 skip |
| offline | PASS：7/7 |

本轮 L3-H 远端证据：H1 session `6a83d67ff5749f0030707814`；Studio primary
`6a83d681f5749f003070784e`；Studio secondary `6a83d681f5749f003070786f`。其他 dev
验证资源由各脚本使用唯一授权名创建并保留作证据，没有执行 production 写入。

## 本轮重点闭环

- 登录凭据只接受严格加密格式；读取缺失 `auth.key` 的凭据不会创建替代密钥；`logout` 可清理损坏凭据；验证登录使用 `--password-stdin`，密码不进入 argv。
- workspace/global/ephemeral 的认证优先级与 session/Studio 无落盘边界已回归。
- manifest/state 使用跨进程锁、revision、成对事务日志和原子落盘；平台写后只合并平台事实或进入显式恢复状态。
- create/publish/合集目录写入具备幂等读取与冲突语义；同版本恢复核对完整不可变发布意图，不再只比较 SHA1。
- JSON/NDJSON、manifest、batch config/report 已有随包机器 schema；发布门禁会执行真实打包 CLI。
- Console 无法直接等价的浏览器支付/签约、上传编辑器、拖拽、SSE/polling 和验证码流程已在 Console 差异文档中明示。

## 未签字与外部前置

| 项 | 状态 | 原因与补齐方式 |
|---|---|---|
| RSS 真实闭环 | 延后 | 按产品决定，功能与 Console 契约已实现；真实验收仍需受控 feed、owner 邮箱和一次性验证码，不能仅凭账号密码伪造 PASS |
| frozen | SKIP | dev API 不能由普通账号制造冻结资源；需 Console 预置自有冻结资源 ID 后重跑 NEG/P6 |
| package 业务验收 | 暂停 | 保持既有范围裁决；模板/兼容门禁仍执行，但不把 package 业务链写成 PASS |
| L3-H TTY 体验 | 人工待签 | 自动化只证明服务与打包命令，不替代真实终端菜单、窄终端和取消操作体验 |
| production | 禁用 | 当前产品契约主动禁止 production，不执行 prod smoke |
| 历史 dev 密码 | 外部待办 | 当前仓库已删除明文；密码曾进入 Git 历史，继续共享或长期联调前必须在账号系统轮换 |

## 结论

当前批准范围的本地门禁、真实 dev 主链、Console payload/表单契约、session/Studio 和打包产物均通过。
RSS、frozen、TTY 与 production 的状态按上表保留，不得表述为已完成真实环境签字。
