# 2026-08-12 dev 验证报告

> 文档角色：日期化运行证据。本文只记录本次执行结果，不定义产品范围或长期完成状态。

## 运行上下文

- 日期：2026-08-12
- 环境：`dev`（`https://api.devfreelog.com`）
- 主账号：`freelog-test11`（`test/.freelog-test-credentials.local.json`，gitignore）
- 辅账号：`snnaenu`（同上，E3 跨 owner 负向）
- CLI commit：`3e5910826601d80aabe635099d690b0de43fb253`
- Console commit：`9cdfac1cf7c56a7c061be8c3fd4bfd43d1ccefc1`（未重跑 console-forms 时可沿用）

## 结果

| 验证层 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `pnpm --filter @freelog-cli/cli test` | PASS：57 文件 / 325 测试 |
| dev 真实业务场景 | `node packages/cli/scripts/verify-scenarios.mjs --env dev` | PASS：**133/133**（0 skip / 0 fail） |
| 全场景入口 | `node test/run-all-scenarios.mjs --env dev` | PASS：scenarios + 10 个 parity 子脚本 |
| 活跃文档治理 | `documentationGovernance.test.ts` | PASS |

## 本次新增/修复覆盖

- **E3** 跨账号 owner 负向（`snnaenu` bind/update → code 2）；`login --global` 切换凭据
- **N-05** JSON schemaVersion=1 envelope；`dep list` / `offline` 等子命令 `command` 字段
- **S8b** `init package --template package-js`；**S14b** batch `--resume` 本地补写
- **DEP-AUTH** 自有资源作依赖须 `dep auth` / batchSignContracts 预检
- **authExcluded 单条 create**：`batchSignContracts` 仅 CLI 预检声明；`createVersion` 在 authExcluded 时不传 batchSignContracts（修复平台格式校验）
- dev 凭据：`test/.freelog-test-credentials.local.json`（gitignore）+ example 模板

## 未进入本次 ENV 结论（已批准跳过）

- **RSS**（C-10 / `FORM-COL-RSS` / RSS-lock）：按产品决策延后；`verify:rss` 未纳入 `run-all-scenarios`
- **prod 环境**：无本次签字证据
- **collect-rules 自动收录触发**：仅有 get/set round-trip 与契约校验，未做受控候选资源专项观察

## 数据说明

脚本在 dev 创建测试资源；本地临时目录会清理，远端资源无统一回收协议。勿将同一脚本指向 production。
