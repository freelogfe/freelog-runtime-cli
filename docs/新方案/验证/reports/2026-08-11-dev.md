# 2026-08-11 dev 验证报告

> 文档角色：日期化运行证据。本文只记录本次执行结果，不定义产品范围或长期完成状态。

## 运行上下文

- 日期：2026-08-11
- 环境：`dev`（`https://api.devfreelog.com`）
- 主账号：`freelog-test11`（凭据未写入仓库）
- 辅账号：仅用于跨账号 owner 负向验证（账号与凭据不写入报告）
- Console commit：`9cdfac1cf7c56a7c061be8c3fd4bfd43d1ccefc1`
- CLI：当前工作区未提交版本；因此本报告不是 commit 级发布签字，提交后应补记 CLI commit。

## 结果

| 验证层 | 命令 | 结果 |
|---|---|---|
| 本地发布门禁 | `pnpm verify` | PASS：tools-lib build、57 个测试文件、309 个测试、typecheck、i18n audit、11 个模板兼容、CLI build、pack dry-run |
| Console 表单源码契约 | `pnpm --filter @freelog-cli/cli verify:console-forms` | PASS：21/21，Console commit 匹配且工作区洁净 |
| dev 真实业务场景 | `node packages/cli/scripts/verify-scenarios.mjs --env dev` | PASS：128/128 |
| Console/API parity | `pnpm --filter @freelog-cli/cli verify:parity` | PASS：10 个子脚本全部通过 |
| collect-rules dev round-trip | `verify-collection-parity.mjs --env dev --case merge0` | PASS：status、serializeStatus、conditionType、filterConditions 写入并回读一致 |
| 活跃文档链接 | `documentationGovernance.test.ts` | PASS：13 份活跃文档均在白名单内，角色声明和相对链接有效 |
| 工作区运行残留 | manifest/state/auth/时间戳工程扫描 | PASS：源码区和 `test/` 无平台绑定项目、凭据或历史 E2E 目录 |
| 补丁格式 | `git diff --check` | PASS |

## 本次覆盖

- 登录、环境和 owner，包括辅账号跨 owner 拒绝；
- 资源创建、文件发布、bump、策略、上下架、listing、草稿和 bind；
- 图片、视频、主题、小说、批量独立资源和合集；
- 合集目录增删改排、merge=0/1、合集属性同步；
- inputAttrs/customPropertyDescriptors 的真实 publish body、平台读回和 REST/SSE 对照；
- dry-run 对新文件、压缩目录和本地 videoCover 的零副作用 `unresolved` 计划语义；
- createBatch、authExcludedItems 单条创建路径、batchSignContracts 和封面解析。

## 未进入本次 ENV 结论

- RSS 绑定/验证码/同步需要可控 RSS 源和对应平台状态，**2026-08-12 仍延后**；见 [2026-08-12-dev.md](./2026-08-12-dev.md)
- collect-rules 已补充 Console 字段契约校验和真实 dev set/get round-trip；自动收录实际触发仍应在有受控候选资源的专项运行中观察
- 浏览器 Network golden 为可选 spot check，本次 parity 使用 Console 源码契约与真实 API，不宣称像素/UI 操作同构。

## 数据说明

真实场景会在 dev 创建测试资源和合集。脚本清理本地临时目录，但当前未提供完整远端资源回收协议；这些对象属于 dev 测试数据，不得把同一脚本直接指向 production。
