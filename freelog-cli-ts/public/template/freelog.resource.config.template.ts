import type { ResourceConfig } from '../freelog.resource';

const config: ResourceConfig = {
  resourceId: '',
  resourceName: '',
  resourceType: [],
  intro: '',
  coverImages: [],
};

export default config;

