fix(cli): dev 真网打通 cookie 鉴权与 create 目标解析

- login 改走 fetch /v2/passport/login（isRemember:1），捕获 Set-Cookie
  （authInfo + uid）；tokenSn 落 token，cookie 加密进 .freelog/auth
- bootstrap 增 getHeaders 送 Cookie；有 cookie 时平台请求带会话
- create 按 Step1 §0.1/§0.3/§4 修目标解析：--file 未登记时挂到
  唯一已有身份；路径必须落在工程内；§0.4 归一化授权标识；
  N.json 已有 typeCode 且未传 --type 时复用工程类型
- 命令层共享旗（--yes/--cwd/--file/--env/--json）经 optsWithGlobals
  透传，修 --yes 与 --password-stdin 同用误报
- test/run-all-scenarios.mjs 重写为新 CLI 真网端到端（primary only）：
  prod 门禁/login/init/create/prepare/1.0.0/策略/online/status/offline
- dev 实测全链通过：resourceId 6a9e40114b5cf5002fa7499d，
  1.0.0 POST 成功删稿，policy on，online 后已 offline 收尾
