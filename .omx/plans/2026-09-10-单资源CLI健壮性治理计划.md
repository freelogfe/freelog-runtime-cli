# 单资源 CLI 健壮性治理计划

日期：2026-09-10  
状态：`[~]` 实施与验收中。此计划记录进度；产品规则以 `docs/一期/产品方案/脚手架设计/` 为准。

相关记录：[当前设计健壮性问题与目标模型](../../docs/一期/产品方案/当前设计健壮性问题与目标模型-2026-09-10.md)、[交接文档](../../docs/一期/产品方案/交接文档.md)。

## 目标和边界

将“一工程多份独立单资源”的 CLI 做成可脚本化、可中断恢复、不会靠标题或目录顺序猜目标的产品。只覆盖普通单文件资源、主题 `RT001` 和插件 `RT002`；不增加合集、批量发行、支付、session、studio、RSS 或本地版本历史。

不变量：`N.json` 是永久身份与产物锚点，`N.version.json` 是唯一未提交工作稿，平台是版本历史真源；`--resource` 选择身份，`--artifact` 表示产物；主题/插件仅目录发版时临时 zip。

## 实施清单

| 阶段 | 状态 | 交付 / 剩余风险 |
|---|---|---|
| 0. 真源与规则统一 | `[x]` | 已移除标题选择、旧 `--file` 与 `artifactMode` 现行语义；正式设计、PHASE、COMMANDS、使用手册与开发说明已同步，非归档残留扫描已完成。 |
| 1. 身份状态 | `[x]` | 完整 `resourceName`、单调 `.sequence`、必填锚点、`N.json` 唯一主本已实现；遗留 `index.json` 忽略并提示。 |
| 2. 选择与路径 | `[x]` | `id:` / 精确 `name:` / `artifact:` / `file:N.json`、最终工程根解析、真实路径和目录嵌套冲突检查已实现。 |
| 3. 事务、锁、诊断 | `[x]` | 可信事务先恢复；新事务只写白名单相对文件；`resource list` 在坏状态下可诊断。正常业务命令仍保守地全量阻断冲突。 |
| 4. init/create/bind | `[x]` | 新增和接续身份不靠第一条猜测；普通/主题/插件锚点预检；bind 与 create 查重兼容 `userId` / `ownerId` / `creatorId`。 |
| 5. 版本与工作稿 | `[x]` | reset 前置校验、属性 review、稳定目录 zip、流式 SHA、Blob 上传与上传中变更核验已实现。 |
| 6. 远端门禁与恢复 | `[~]` | 现有资源的写入口统一环境、ID、owner、冻结门禁；版本 POST 有 pending/recover 精确 SHA 闭环。create、listing、策略、上下架、依赖签约的未知结果尚未承诺 recovery，先评估平台可证明性。 |
| 7. listing、策略、依赖、上下架 | `[~]` | listing 三值/封面上传、模板分页、依赖策略显式选择、写门禁均已实现。模板暂请求平台全部返回，等待后端修复类型筛选及事件编译契约；届时再设计参数填写。依赖维持“已有授权或签约成功即写稿、支付在浏览器端”的已确认边界。实际跨平台 TTY 已验；其余风险是非版本写操作的未知结果恢复，以及资源池暂不能重演 primary 的未授权付费签约。 |
| 8. 大文件与可观测性 | `[~]` | 流式 SHA、Blob 上传、稳定 zip、链接拒绝已完成；显式大小上限、取消协议和完整 JSON 输出契约尚未设计。 |
| 9. 场景、发布包、真网验收 | `[~]` | 最新本地回归为 30 文件、168 项测试、typecheck、build、发布包验证与 diff 检查，均已通过。最终 dev 聚合已实跑：场景、字段、资源池依赖、属性+文本/下拉配置同稿的首版/更新版读回、TTY 与工作稿安全均 PASS。可选配置 DTO 以后台嵌套 `resourceConfig.supportOptionalConfig` 为权威。最终仍 BLOCKED 的仅是后端事件策略 DSL 契约，以及资源池没有 primary 未授权的启用付费策略可重演签约；版本 POST 未知结果的真网可控中断也仍缺证据。 |

## 每一项的工作方式

1. 新的产品取舍先说明用户场景、命令、本地状态、平台事实、异常恢复、普通资源/主题/插件差异和测试；等待用户复核。
2. 已确认规则的修复仍遵守“先正式设计，后代码，后测试”。
3. 每个写入口都审计零/一/多身份、歧义、非交互、`--yes`、坏状态、环境、owner、冻结、远端结果未知和本地收尾中断。
4. 不以单个领域绿测或历史 dev 报告推断交付完成。

## 最终验收

```powershell
pnpm --filter @freelog-cli/cli2 test
pnpm --filter @freelog-cli/cli2 typecheck
pnpm --filter @freelog-cli/cli2 build
pnpm --filter @freelog-cli/cli2 verify:package
git diff --check
node test/run-final-acceptance.mjs --env dev
```

除自动化外，dev 场景至少保留普通文件、主题目录、主题 zip、插件目录、已有本地工程、bind、多资源同目录和版本提交结果未知恢复的可复核证据。凭据和 token 不得写入任何输出或仓库文件。
