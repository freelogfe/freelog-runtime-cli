/**
 * Freelog 资源配置文件
 * @type {import('../freelog.resource').ResourceConfig}
 */
const config = {
  resourceId: '',
  resourceName: 'my-freelog-project',
  resourceType: ["主题"],
  intro: '',
  coverImages: [],
};

export default config;

